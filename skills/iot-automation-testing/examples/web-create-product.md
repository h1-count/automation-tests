# Web 示例：创建产品

状态：示例，未作为当前环境的已验证执行结果。

## 输入

- 已确认的开放平台测试环境。
- 产品管理需求或页面 URL。
- 目标：使用合法产品信息创建产品，并在产品列表中查询到该产品。

## 流程示例

1. 先输出测试计划，确认目标环境、产品字段、权限和测试数据清理方式。
2. 确认后生成 `OPEN-PLATFORM-PRODUCT-001` 用例，初始状态为“草案”。
3. 用例确认后，使用 Playwright 模板生成脚本，优先使用 `product-create-*` 的 `data-testid`。
4. 脚本与执行方案确认后运行 `npm run test:web`。
5. 失败时保留 Playwright Report、截图和 Trace，并按失败分类规范输出分析。

## 仍需由项目补充的信息

- 真实字段规则和成功提示。
- 稳定 `data-testid`。
- 测试账号与测试数据准备/清理方式。
