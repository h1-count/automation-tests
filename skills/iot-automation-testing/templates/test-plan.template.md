# 测试计划：<计划标题>

> 填写结构以 [流程规范](../../../docs/testing/automation-guideline.md)、[用例规范](../../../docs/testing/testcase-guideline.md)、[环境规范](../../../docs/testing/environment-guideline.md)、[定位规范](../../../docs/testing/selector-guideline.md) 和 [报告规范](../../../docs/testing/report-guideline.md) 为准。

> 表格写入约束：同一张表的表头、分隔行和每条数据行保持相同列数；单元格内换行使用 `<br>`，字面量竖线写为 `\\|`。生成后必须先对实际计划运行 `npm run check:markdown -- <计划路径>`，再通过 `npm run check:architecture`，才可展示或提交确认；仅分隔行不一致时可用 `--fix` 修复后复检。

> 结构版本：case-relation-projection-v1。原子用例的“用例编号”是身份来源；仅“规则覆盖台账”的“关联 caseId”可人工维护。其余 `caseId` 列由 `npm run testcases:sync-relations -- <runtime 暂存请求目录>` 生成并经 Activity 原子发布，请勿直接修改最终请求目录。

> 结构版本：web-script-governance-v1。新建或重新打开执行范围的 Web/H5 请求在工程层记录场景组和证据策略；历史请求不迁移。

> 结构版本：page-session-group-v1。

> 结构版本：case-evidence-policy-v1。

## 基本信息

| 项目 | 内容 |
| --- | --- |
| 计划编号 | <计划编号> |
| 测试类型 | Web / App / API / MQTT / IoT 链路 |
| 目标环境 | 待确认 |

## 正式用户决定

> 本表是正式用户决定内容的唯一来源，只记录已作出的决定及其适用 `subjectDigest`；等待、重试、失效和 callback 生命周期只在 `workflow-history.ndjson` 中维护。没有决定时填写“无”，不得用“待确认”伪造一条决定。

| 决定类型 | subjectDigest | 正式决定 | 决定内容与适用范围 | 后续处理 |
| --- | --- | --- | --- | --- |
| 计划确认 / 用例确认 / 执行清单确认 / 业务裁决 / 正式资产变更 | <SHA-256 / 无> | accepted / rejected / revision_requested / cancelled / 无 | <用户明确决定及受影响范围 / 无> | <继续对应下游 / 修订 / 取消 / 无> |

## 测试范围

> 这里只列注册、登录、产品管理等顶层业务流程及明确排除项；字段规则、验证码、断言文案、原子用例拆分和 `caseId` 分别写入需求追溯、规则设计、用例与评审区块，不得混入范围条目。

### 包含

- <范围项>

### 不包含

- <范围项>

> “不包含”仅记录明确的业务不适用、用户明确排除或超出本次测试请求的范围；不得因实现复杂、资料缺失、环境暂不可用或需要受控执行而排除需求定义的流程。此类事项应保留在“覆盖矩阵”“待补充信息”或“风险与审核事项”中。

## 输入资料

> 结构版本：source-reference-links-v1

> 本表是本次计划的**实际引用资料清单**，不是候选资料目录。仅记录已打开且用于范围、业务规则、推断或结论的资料；章节索引命中但未读取的资料不得写入。仓库内资料必须使用可点击的相对 Markdown 链接；原型链接到精确页面，PDF/Word 在“页码、标题或页面定位”中保留定位信息。每次向用户输出计划、用例草案或评审结论时，同步展示本表中本轮实际使用的条目；未引用知识库资料时明确说明。对话附件未落盘时填写“仅对话附件，暂无持久链接”，不得伪造链接。

| manifest id / sectionId | 资料类型 | 可点击资料链接 | 页码、标题或页面定位 | 版本 / SHA-256 | 本次用途 |
| --- | --- | --- | --- | --- | --- |
| <sources/manifest.yaml 中的 id；章节索引命中且实际读取时补充 sectionId；对话直接资料填“待补登”> | <需求文档/截图/原型/接口文档等> | `[资料名](../../../../sources/<path>)`；对话附件填“仅对话附件，暂无持久链接” | <页码、标题、原型页面或不适用> | <版本、SHA-256 或待确认项> | <用于范围/业务规则/字段/交互/推断/待补充判断> |

## 测试资产选择

<!-- testcase-standard: test-asset-selection-v1 -->

> 本表只记录从 `test-assets/manifest.yaml` 选择的静态测试资产，不是原始需求、输入资料或需求追溯。计划阶段可运行 `npm run test-assets:select -- --project <项目> --kind <类型>`；已选择 App 包时运行 `npm run check:environment -- --plan --asset <assetId>` 验证文件完整性。包名、Activity、Appium、设备和安装状态属于工程设计验证，不阻塞计划或用例生成。

| assetId | 类型 | 可点击资产链接 | 选择依据 | 选择状态 | 工程验证项 |
| --- | --- | --- | --- | --- | --- |
| <test-assets/manifest.yaml 中的 assetId / 不适用> | <app-package / firmware-package / visual-baseline> | `[资产名](../../../../test-assets/<path>)` / 不适用 | <唯一候选 / 唯一默认候选 / 用户选择 / 不适用> | 已选择候选 / 待选择 / 不适用 | <包名、Activity、Appium、设备、安装状态或不适用> |

## 环境选择

> 结构版本：environment-status-v1

> 计划阶段先运行 `npm run check:environment -- --plan`；已选择静态资产时追加 `--asset <assetId>`。本表只记录脱敏状态，不记录地址、账号、密码、Token、Cookie 或会话内容。“已配置”不等于已预检，“已预检”不等于用户已确认或业务服务可用。静态包已选但 Appium/设备未验证时，在工程任务记录待验证，不得阻塞用例生成。

| 候选环境 | 选择依据 | 本地配置状态 | 预检状态 | 正式决定引用 | 未闭合项 |
| --- | --- | --- | --- | --- | --- |
| test | <用户未指定环境时默认；用户指定、已确认计划或已确认用例可覆盖> | <已配置 / 部分配置 / 未配置 / 未读取> | <已预检 / 预检受阻 / 未预检> | <正式用户决定行 / 无> | <认证会话、账号可用性、验证码人工接管或无> |

## 测试方式

- <Web / App / API / MQTT / IoT 链路>

## 测试数据策略与残留台账

> 结构版本：test-data-policy-v2

> 按[环境规范](../../../docs/testing/environment-guideline.md)选择数据策略，并记录本请求的引用与确认结论。

| 数据策略 | 允许环境 | 资源类型 | 最大数量 | 关联覆盖范围 / caseId | 正式决定引用 |
| --- | --- | --- | --- | --- | --- |
| no_write / ephemeral_cleanup / reusable_fixture / tracked_residual | test / pre / 不适用 | <无 / 企业申请 / 产品 / 文件等> | 0 / <正整数> | <计划阶段填写覆盖范围；生成后回填 caseId / 不适用> | <正式用户决定行 / 不适用> |

### 写入策略明细

- <策略名称>：关联覆盖范围 <业务动作或资源场景>；生成原子用例后回填 `caseId`；唯一合成标识 <规则>；基线合约 <baselineContractId / 不适用>；租约 <shared_read / exclusive / 不适用>；恢复/退役策略 <规则 / 不适用>；TTL <小时 / 不适用>。

- 台账或数据策略引用：<环境规范约定的登记点 / 不适用>。
- 残留风险与处理期限：<无 / 后续处理条件与期限>。

### 执行清单映射

| caseId | 正式脚本 | 允许操作 | 资源类型 | 数量预算 | 后台验证 | 敏感产物策略 |
| --- | --- | --- | --- | --- | --- | --- |
| <caseId> | <*.formal.spec.ts> | <发送测试验证码 / 上传合成文件 / 提交 / 创建 / 查询 / 清理> | <资源类型 / 无> | <正整数 / 0> | <已登记 API / 管理页 / 对应断言受阻> | <遮罩 / 暂停采集 / 常规> |

## 覆盖基准与拆分清单

> 结构版本：coverage-inventory-v1

> 在生成用例草案前填写。本区块与需求追溯、覆盖矩阵和评审结论共同保存在当前 `plan.md`，不另建说明文档。按业务风险拆分，不使用固定用例数量配额，也不预分配或承诺最终 `caseId` 数量；无对应资料或场景时填写“不适用及依据”。

### 基准资料与模块映射

| 基准资料 | 层级或模块/流程 | 可验证场景或规则 | 计划覆盖范围 | 关联 RULE | 派生 caseId | 差异、不适用或待补充依据 |
| --- | --- | --- | --- | --- | --- | --- |
| <需求文档 / XMind / 人工用例；无则填写“无”> | <L1/L2/必要时 L3，或模块/流程> | <主成功、失败、回退、重试或具体规则> | <业务规则、风险或场景；非 caseId> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |

### 拆分清单

| 类别 | 拆分项与适用规则 | 计划覆盖范围 | 关联 RULE | 派生 caseId | 不适用、待补充或合并依据 |
| --- | --- | --- | --- | --- | --- |
| 输入字段 | <字段；必填、类型、格式、长度、范围、关联约束、触发动作> | <业务规则、边界策略或风险> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |
| 枚举与状态 | <数据态、等级、状态迁移、Tab、视图或规则分支> | <业务规则、合法/非法转移或风险> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |
| 关键交互 | <入口、跳转、弹窗、确认/取消、返回、保存、删除、授权> | <用户旅程或关键断言> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |
| 角色与权限 | <角色－动作－资源；允许与越权拦截> | <允许路径、越权拦截或待确认项> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |

### 测试设计技术与依据

> 仅对适用场景填写；所有记录均在本 `plan.md` 中维护，不另建平行设计文档。

| 适用场景 | 设计技术与最小记录 | 需求追溯编号 | 计划覆盖范围 | 关联 RULE | 派生 caseId | 不适用或待补充依据 |
| --- | --- | --- | --- | --- | --- | --- |
| 多条件业务规则 | 决策表：<条件、结果、有效/无效组合> | REQ-<模块>-001 | <待阶段二按组合拆分> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |
| 生命周期与状态机 | 状态迁移：<当前状态、事件、允许/禁止目标状态> | REQ-<模块>-001 | <待阶段二按转移拆分> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |
| 复杂输入 | 等价类/边界策略：<分区；下限前/下限/上限/上限后；组合约束> | REQ-<模块>-001 | <待阶段二按风险拆分> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |
| 核心用户旅程 | 场景法：<主成功、失败、回退、重试> | REQ-<模块>-001 | <待阶段二按场景拆分> | <RULE-... / 待阶段二> | <由同步命令生成> | <依据> |

## 覆盖矩阵

> 先按需求、风险、变更和环境判断适用性，不要求每次测试覆盖全部通用域。仅“适用”的域需要生成关联用例；“不适用”和“适用待补充”必须说明依据或影响。

| 覆盖域 | 适用性与依据 | 计划覆盖范围 | 结论 | 派生 caseId |
| --- | --- | --- | --- | --- |
| 业务功能与规则 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <阶段一填“待阶段二生成”；阶段二回填 caseId> |
| 输入与数据校验 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 状态与生命周期 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 数据完整性与一致性 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 异常、容错与恢复 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 权限、身份与审计 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 安全与隐私 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 接口、集成与契约 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 兼容性与可移植性 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 交互、视觉与无障碍 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 性能、容量与稳定性 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 配置、部署与可运维性 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |
| 本地化与法规要求 | 适用 / 不适用：<依据> | <待填写> | 已覆盖 / 适用待补充 / 不适用 | <用例编号或待生成> |

## 需求追溯矩阵

> 覆盖矩阵评估测试维度；本矩阵逐条映射需求。每项适用 P0 必须关联至少一条用例。高风险或资料不足的需求保留为“适用待补充”或“受控执行”，不得写入“不包含”。

| 追溯编号 | 需求来源与版本/章节 | 优先级 | 可验证业务规则 | 适用性 | 计划覆盖范围 | 派生 caseId | 覆盖状态 | 缺失信息或执行门禁 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| REQ-<模块>-001 | <资料路径；版本；章节> | P0 / P1 / P2 / 未标注 | <单一可验证规则> | 适用 / 不适用 / 超出本次请求 | <计划阶段填写覆盖范围> | <由同步命令生成> | 已覆盖 / 适用待补充 / 受控执行 | <依据、阻塞或门禁> |

## 规则覆盖台账

> 结构版本：rule-coverage-v1

> 每一行是来自实际读取资料的原子可测试规则，不由代码反推。“关联 caseId”是唯一人工维护的 RULE 覆盖关系源；不适用、待补充、待用户裁决和受控执行必须有业务依据、执行门禁或最小裁决问题。

| 规则编号 | 需求追溯编号 | 来源定位 | 规则类型 | 触发条件/输入 | 可观察预期 | 设计证据 | 适用性 | 覆盖状态 | 关联 caseId | 依据、执行门禁或裁决 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RULE-<模块>-001 | REQ-<模块>-001 | <实际资料链接、章节/页面/字段> | 业务规则 / 分支/决策 / 输入边界 / 异常与恢复 / 状态流转 / 页面交互 / 权限/身份 / 集成与数据一致性 | <条件、输入或事件> | <可断言页面、接口或数据结果> | <决策表 / 等价类与边界 / 状态迁移 / 交互断言 / 异常路径或恢复路径> | 适用 / 不适用 / 待补充 / 受控执行 / 待用户裁决 | 已覆盖 / 受控执行 / 待用户裁决 / 不适用 / 待补充 | <阶段二回填 caseId；受控执行也必须填写> | <业务依据、环境/数据/安全门禁或最小裁决问题> |

## 规则设计矩阵

> 结构版本：rule-design-matrix-v1

> 仅从已引用 RULE 派生。资料未定义格式、文件属性、成功页或恢复语义时写“未定义，不生成断言”并保留资料定位；不得用泛化的“已定义校验”代替可观察预期。生成或修订用例后运行 `npm run check:rule-design -- <plan.md>`。

| 规则编号 | 字段或状态对象 | 必填/选填 | 有效、无效或边界输入 | 可观察预期 | 数据前置 | 执行门禁 | 关联 caseId | 结论 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| RULE-<模块>-001 | <字段组 / 状态对象> | 必填 / 选填 / 不适用 | <有效、无效、边界；未定义时说明> | <明确页面、接口或数据观察；未定义则不生成断言> | <已有数据、会话或无写入前置> | <无 / 验证码 / 写入授权 / 权限等> | <实际 caseId> | 已覆盖 / 受控执行 / 用户裁决 / 不适用 |

## 规则邻域复核

> 每次自动演进把触发发现项映射到 RULE 后，复核同一 REQ、字段或状态对象、提交/状态路径及数据前置组的全部适用规则；资料明确的遗漏一次性修订并复检。

| 触发发现项 | 邻域 RULE | 共同依据 | 修订 caseId | 关系同步与规则设计预检结果 |
| --- | --- | --- | --- | --- |
| <MRR-... / 初始生成预检> | <RULE-...> | <同一 REQ、字段组、路径或前置组> | <caseId / 无> | <通过 / 待处理> |

## 用例包目录

> 用例包按业务模块或完整流程组织。计划阶段只定义包边界和覆盖范围；阶段二生成原子用例后才回填稳定 `caseId`，并以它而非文件路径作为追溯主键。

| 用例包 | 覆盖模块或流程 | 计划覆盖范围 | 实际原子用例编号 | 特殊门禁 |
| --- | --- | --- | --- | --- |
| `cases-<module>.md` | <模块或流程> | <字段边界、状态或权限等> | <由同步命令生成；尚未生成填“无”> | <无 / 高风险确认 / 独立数据等> |

## 变更影响分析

> 无变更时填写“无”；有变更时先全量复核需求追溯矩阵和覆盖基准与拆分清单，再记录受影响资产。局部变更仅将受影响用例退回草案；范围发生实质变化时建立新的测试请求。

| 变更编号 | 来源与版本 | 受影响需求追溯编号 | 影响判定与依据 | 受影响 caseId | 脚本/工程设计影响 | 数据/环境影响 | 是否复测与结论 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| <CHG-001 / 无> | <需求、接口、页面、代码或风险来源；版本> | <REQ-... / 无> | <局部 / 实质范围变化 / 无影响；依据> | <用例编号 / 无> | <脚本路径、工程层区块或无> | <准备、清理或无> | <是/否；待确认/已完成> | 草案 / 已确认 |

## 合理推断

- <基于用户描述或配置的推断；没有则填写“无”>

## 待补充信息

- <缺失信息；没有则填写“无”>

## 风险与审核事项

- <风险或需要人工审核的事项；没有则填写“无”>

## 多角色评审记录

> 结构版本：multi-role-review-v1

> 结构版本：evidence-driven-evolution-v1

> 结构版本：auto-evolution-loop-v1

> 结构版本：knowledge-decision-v1

> 按[用例规范](../../../docs/testing/testcase-guideline.md)和 [Skill 评审提示卡](../SKILL.md#多角色隔离评审提示卡)填写输入基线、适用角色、正式结论与发现项。这里是结论和发现项正文的唯一正式来源；reviewer 派发、提交、等待与批次失效只写入 `workflow-history.ndjson`，宿主任务标识只写可丢弃 runtime。

> `case-review-resolution` 只更新本区块的 reviewer 正式结论、发现项和评审汇总结论；`REQ/RULE`、设计矩阵、关系与用例草案的证据驱动修订统一由后续 `case-review-evolution` 发布。

<!-- review-batch:REV-<序号>:start -->

### 评审批次：REV-<序号>

| 字段 | 内容 |
| --- | --- |
| 触发类型 | 常规 / 高风险 / 变更 |
| 输入基线版本 | <原始资料版本；plan.md 版本；用例包版本；静态检查结果版本> |
| 适用角色及依据 | <角色清单；每个角色的风险或变更触发依据> |
| 复审来源 | <初审 / 触发发现项或变更编号 / 无> |
| 隔离规则 | <仅向本角色提供原始资料、当前 plan.md、用例包和角色检查清单；未提供作者推理、自评或其他 reviewer 结论；只读> |
| 综合结论 | 可提交确认 / 需演进 / 阻塞 |

#### reviewer 正式结论

| 角色 | 角色输入基线 | 结论 | 发现项编号 | 正式结论摘要 |
| --- | --- | --- | --- | --- |
| <需求一致性 / 测试设计 / 追溯审计 / 交互与状态 / 变更影响> | <资料、plan.md、用例包与静态检查摘要> | 通过 / 需演进 / 阻塞 | <MRR-... / 无> | <证据支持的结论；不写任务标识或运行状态> |

| 发现项编号 | 角色 | 证据 | 发现项分类 | 受影响 REQ/caseId | 严重度 | 处置方式 | 修订/裁决证据 | 关闭状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| MRR-<角色>-001 | <角色> | <资料章节、计划表行、用例包或静态检查证据> | 需求覆盖缺口 / 资料明确的设计缺口 / 业务裁决/资料冲突 / 质量建议 | <REQ-... / caseId / 无> | 高 / 中 / 低 | 自动演进 / 用户裁决 / 风险登记 | <自动演进须填写资料证据和已修订 caseId；用户裁决须填写冲突、最小问题和未裁决状态；风险登记须写“非本次验收阻塞”> | 待处理 / 待用户裁决 / 已关闭 / 不适用 |

#### 沉淀判定

> 按[用例规范](../../../docs/testing/testcase-guideline.md#需求缺口处置与沉淀判定)填写。需求事实不得写入项目经验库；未验证项目观察在本地候选队列生成 `CAND-<编号>`，本计划只引用该编号；已验证经验直接引用项目经验库位置。无发现项且无候选时填写一行“无”。

| 发现项编号 | 归属类型 | 目标位置 | 证据状态 | 处理结果 |
| --- | --- | --- | --- | --- |
| <MRR-... / EXP-CAND-001 / 无> | 需求事实 / 通用规则 / 项目经验候选 / 项目经验 / 未验证推断 / 无 | <REQ → RULE → caseId / testcase-guideline.md / CAND-<编号> / docs/testing/knowledge/<project>-testing-knowledge.md / 当前 plan.md / 无> | 资料已确认 / 待受控探索或正式执行验证 / 受控探索已验证 / 正式执行已验证 / 无 | 已回链 / 已引用或已更新 / 已提升 / 待验证 / 风险登记 / 待用户裁决 / 无 |

<!-- review-batch:REV-<序号>:end -->

## 用例集评审与演进

> 用例草案完成后、用户确认前填写；每次修改用例集后重新评审。

- 需求追溯统计：<总数；适用数；已关联数；适用待补充数；受控执行数>。
- 规则覆盖统计：<适用规则、已覆盖、受控执行、待用户裁决、不适用、无映射规则；按规则类型列出适用数与缺口>。
- 覆盖域复核：<适用域及其关联用例；不适用域及依据>。
- 覆盖基准与拆分复核：<模块/流程、输入字段、枚举/状态、关键交互、角色权限的关联结论；XMind 或人工基准差异及处理结论>。
- 测试设计技术复核：<决策表、状态迁移、等价类/边界策略和场景法的适用结论及关联 caseId>。
- 变更影响复核：<变更来源、受影响需求/用例/脚本、复测结论；无变更则填写“无”>。
- 多角色评审复核：<评审批次、适用角色及触发依据、各角色结论和发现项关闭情况>。
- 发现的缺口与演进：<自动演进的追溯编号、资料证据、已新增或修订的草案用例；用户裁决的最小问题和限定范围；非本次验收阻塞的风险登记>。
- 评审结论：可提交确认 / 需演进。

## 工程层：代码定位与自动化设计

> 仅在计划和适用用例均已确认后填写。该区块复用同一 `plan.md`，将 `caseId` 映射到代码、图谱、测试能力和脚本方案；不得改写业务层已确认的范围、步骤或预期。

| 项目 | 内容 |
| --- | --- |
| 代码仓库 | `.local/repositories/<repository>` |
| 仓库确认依据 | <项目名、资料、URL 或已确认资产> |
| 分支/提交标识 | <分支；提交 SHA 或未获取原因> |
| Graphify 图谱 | <路径 / 未发现 / 不可用> |
| 图谱新鲜度 | 匹配 / 可能过期 / 无法判断；<依据> |
| 源码确认范围 | <相关页面、路由、接口、状态或直接依赖路径> |

| caseId | 需求追溯编号 | 源码路径与定位依据 | 可复用能力 | 自动化结论 | 脚本与断言方案 | 数据/环境前置条件 | 风险或待确认项 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| <系统-模块-001> | REQ-<模块>-001 | <路径；图谱或源码依据> | <action/client/fixture/support> | 可实现 / 部分自动化 / 受阻 / 需求或代码差异待确认 | <脚本路径；核心断言> | <准备、清理、权限、设备> | <风险或无> |

### 规范差异与用户告知

> 仅在发现不符合自动化规范的事实时填写，并在同轮回复向用户简短说明；不得只写“无法自动化”。每项说明问题类别与具体证据、受影响 `caseId`、自动化影响和最小处理建议。

| 差异编号 | 问题类别 | 具体事实与证据 | 受影响 caseId | 自动化影响 | 最小处理建议或待确认项 | 自动化结论 | 用户已告知 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| ENG-GAP-001 | 可访问性/定位 / 测试契约/接口 / 复用能力 / 数据、环境或权限 / 仓库或代码定位 | <例如：按钮缺少唯一 ARIA name、关联 label 和 data-testid；候选定位匹配 2 个元素> | <系统-模块-001> | <无法生成稳定 selector / 不可安全执行 / 需业务确认> | <补充 data-testid、提供测试账号或确认接口契约> | 部分自动化 / 受阻 / 需求或代码差异待确认 | 是；<本轮回复摘要> |

### 脚本与证据方案

- 拟修改或新增文件：<脚本路径及目的>
- 执行命令与目标环境：<命令；环境>
- 定位证据与可达边界：<source_verified / runtime_verified / visible Inspector fallback / runtime_validation_pending；源码版本与路径；验证命令；reachableBoundary；待正式执行验证范围>
- 预期报告位置与逐用例证据：<artifacts 路径；CaseEvidenceBundle、关键截图、视频区间、Trace 或协议摘要>
- 差异与审核事项：<代码与需求不一致时，列出证据、影响 caseId 和需业务层确认的事项；无则填写“无”。>

#### 页面场景组映射

> 按[环境规范](../../../docs/testing/environment-guideline.md#611-pagesessiongroup-与-context-隔离)填写；只记录会话调度和复位设计，不复制业务步骤。

| sessionGroupId | targetRoute | caseIds | resetStrategy | isolationReason | executionOrder |
| --- | --- | --- | --- | --- | --- |
| <GROUP-001> | <页面路径> | <caseId 列表> | <清空字段、关闭提示、恢复依赖字段并验证基线> | <同页复用依据 / 新 Context 原因> | <异常 → 边界 → 正常 / 独立> |

#### 证据策略映射

> 按[报告规范](../../../docs/testing/report-guideline.md#41-逐用例可审计证据包)填写证据类型与脱敏策略；正式结果不写入计划。

| caseId / 场景组 | 证据策略 | 关键截图 | 视频或 Trace | 网络/控制台摘要 | 敏感步骤处理 |
| --- | --- | --- | --- | --- | --- |
| <系统-模块-001 / GROUP-001> | 审计均衡 | <基线；最终断言> | <视频区间；失败 Trace> | <适用的脱敏摘要 / 不适用> | <遮罩、暂停或安全替代证据> |

## 预计交付物

- 测试用例：<计划确认后生成的用例文件>
- 工程层设计：<用例确认后补充到本 `plan.md` 的代码/图谱定位与可行性评审>
- 自动化脚本：<工程层设计确认后生成的脚本文件>
- 执行报告：<脚本审核和执行确认后生成>
