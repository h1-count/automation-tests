# 用例集：开放平台高级配置（product-advanced）

> 测试类型：Web ｜ 默认环境：test ｜ 默认数据策略：no_write
>
> 上下文来源：`sources/indexes/open-platform.yaml`（product-access-create 命中章节，2026-09-11 重读）、`sources/open-platform/需求/产品接入系统功能需求说明/产品接入系统功能需求说明.md`（「高级配置」章节 L1016-1103 与「产品开发流程」L116/L324-340，2026-09-11 重读）、前端源码 `web-open-platform/src/pages/integration/product/ProductPhaseAdvanced.vue`（卡片分发/面包屑/文档新窗口 L4-30/L81-112）、`AdvSceneConfig.vue`（触发/执行两表、申请上线前置校验 L305-348、删除确认 L350-367、validFlag 行 L28-33）、`AdvMessageConfig.vue`（表列/推送状态开关 disabled L57/编辑 disabled L66/删除确认 L128-141/开关切换 toast L143-162）、`components/BtnCreateMessageFromTpl.vue`（弹窗标题/提示 L2-6、空选拦截 L109-128）、`components/AdvancedConfigContainer.vue`（页脚「返回上一级」L20-34/L87-99）、`components/AdvancedItemCard.vue`、`components/DialogTrigger.vue`（确定链路 loading 防重复 L54-65/L206-214）、`src/routes/integration.ts`（advanced/scene/message 子路由与 permission meta L106-133）、`src/support/recordGeneratedData.ts`、`experience/general.md`、`experience/open-platform.md`、create-product 包台账 `testpacks/web/open-platform/create-product/runtime/generated-data.json`；查重比对 authcode/procurement 包（申请上线类动作不重复）。
>
> 上下文结论：**2026-09-11 按对象矩阵核对规则重新生成**——本轮请求为「测试开放平台产品编辑链路」：**不创建产品**，直接使用 create-product 功能包台账中已创建的既有合成产品进行编辑，操作对象为台账最新一条记录（2026-09-10 创建：自动化测试产品1789026290672 / 型号 at0672 / model at0635.light.at0672 / 照明-灯 / 开放协议接入 / 普通设备）；「创建产品」相关对象一律按范围外声明或改为基于既有产品的场景。既有探索实证（2026-09-03，product id=2095414624647254016/at8438，结构类结论继续有效）：①高级配置首页（/integration/product/{id}/advanced）为卡片列表（场景联动配置/消息推送配置，均「可选」徽标），顶部面包屑（点卡片名/面包屑可返回）+「点我查看」新窗口文档链接（el-link @click `window.open(_blank)`，目标 `/service-support/home?path=product-development/...-define`，路由 auth:false 免授权）；②场景联动页 = 触发条件设置 + 执行动作设置两个表（列：自动化名称/触发属性事件(或执行属性方法)/样式类型/触发范围(或执行范围)/自动化类型[标准|自定义]/状态[开发中|审核中|已上线]/操作[查看/编辑/驳回原因?/删除?]）+「新建自动化」按钮（仅触发区右上）+ 金云智居App图例区；行级规则（源码）：「删除」仅对 开发中且自定义 行开放、编辑在 `!validFlag || itemStatus!=='developing'` 时禁用、`lastApproveStatus===3` 出现「驳回原因」、`validFlag===0` 行红底+警示图标（tooltip「该场景配置已失败，请立即删除。」）；页脚按钮组=「返回上一级」(`router.back()`)+「申请上线」（`doAction('online')` 前置产品状态 已上线/开发中，否则 alert 提示；applySceneConfigOnline 错误码 81006/50040 warning 分支，成功 toast「上线申请已提交」——提交动作范围外，本包仅断言在位）；③消息推送页 = 「套用模板」+「新建推送」按钮 + 表 8 列（消息推送标题/内容/触发条件/推送方式/审核状态[审核中|审核成功|审核失败]/推送间隔(秒|分)/推送状态[开关，`approveStatus!==2` disabled]/操作[查看/编辑(审核中 disabled)/删除]）；套用模板弹窗（标题「套用消息推送模板」，宽 1200）alert「选用标准消息,无需审核即可上线;根据功能定义已自动匹配以下标准消息。」+ 模板表（消息文案[标题/内容]/触发条件/推送规则[推送方式/推送间隔/用户可关闭]，多选），确定空选拦截「请选择至少一个模板」且弹窗不关闭（doConfirm 返回 false），成功 toast「新增成功」；删除确认「确认删除模板消息 {标题}」（标题：删除消息模板），成功 toast「删除成功」；DialogTrigger 确认链路请求期 loading 禁用确定/取消按钮（防重复提交）；④写入目标为 create-product 台账最新产品（本轮 at0672，生命周期约定见 product-management conclusion），写入记录经 `src/support/recordGeneratedData.ts` 入本包 `runtime/generated-data.json`（kind=open-platform-message-tpl / open-platform-message-tpl-delete，只增不删）；⑤环境事实（2026-09-03 实证）：test 环境灯品类在线标准消息模板列表 totalCount=0，写入类用例按约定注解转跳；高级配置子页为懒加载 chunk，dev 首访冷编译约 9s，脚本以就绪文本硬等而非固定睡眠。
>
> 范围外声明：
> - 「新建自动化」完整表单链路（BtnEditSceneConf 931 行：自动化类别/功能选择/样式类型/数值型比较符与范围跨字段校验，表单组合复杂度高，单独用例集另行设计）；本包仅覆盖入口在位与列表结构（OP-PADV-002）。
> - 「新建推送」「查看」「编辑」完整表单（BtnEditMessageTemplate 672 行模板编辑器，含触发条件编辑器）另行设计；行内入口随列表数据注解观察（OP-PADV-003 步骤3）。
> - 「申请上线」提交流程（场景联动页页脚全局提交动作，前置产品状态 已上线/开发中，错误码 81006/50040 分支源码已核对）：影响产品生命周期，归属产品上线流程非本包；本包仅断言按钮在位且不点击（OP-PADV-002 步骤5）。
> - 消息行状态驱动控件样本（推送状态开关切换 toast「消息模板状态切换成功」、审核中行编辑禁用、审核状态三态 tag 行级渲染）：源码禁用/渲染规则已核对（AdvMessageConfig.vue L35-41/L57/L66、updatePushStatus L143-162），但行级样本依赖含审核状态的消息行，待确认：环境在线标准模板与审核状态数据；现状由 OP-PADV-003 步骤3 注解观察。
> - 场景联动标准自动化数据行（由功能点自动生成，依赖产品功能定义数据）与 validFlag=0 失败行样本：无法在测试内主动构造，OP-PADV-002 步骤3 以注解观察；删除确认弹窗（「删除操作不可恢复，请确认删除。」）依赖开发中自定义行样本，同注解观察，不单独设写入用例。
> - 未登录直达 /advanced 及子页（负路径｜绕过入口直达）：路由 `permission: '/integration'` meta + 全局 beforeEach 守卫为平台级机制，create-product 包 OP-PROD-001 已实证同树路由未登录重定向登录页；登录态用例属 login-register/create-product 包，本包运行时复用 `runtime/auth-state.json`。
> - 套用模板「确定」重复提交（连点/请求期重复触发）：源码实证 DialogTrigger doConfirm 请求期 loading 置位并禁用确定/取消按钮（btnConfirmDisabled/btnCancelDisabled），成功后弹窗关闭无重复提交窗口；构造连点窗口需在线模板数据且违背最少写入原则，不另设用例。
> - 消息模板删除的二次重复触发：删除成功后行消失、行内删除入口不复存在（doDeleteTpl 成功后 refresh），同一对象无法二次删除；服务端幂等语义需求未定义，无法稳定构造。
> - 消息行级取值渲染（推送方式多值字典渲染、推送间隔 秒/分 单位、内容 3 行截断省略）与套用模板弹窗分页（模板表 auto-pagination pageSize=10）：待确认环境需存在对应数据量级的在线标准模板，暂不设计；表头/字段结构核对已由 OP-PADV-003/005 覆盖。
>
> 待确认：环境在线标准消息模板数据（test 环境灯品类 2026-09-03 实证 totalCount=0）——影响 OP-PADV-004 写入路径执行与 OP-PADV-005 弹窗数据行展示，缺失时按用例约定注解转跳（skip，不判失败）。
>
> 本表供测试范围审核。真实密码、Token、密钥、Cookie 等可复用凭据仅从本地 `.env` 或对应登录态存储读取；测试消息模板为平台标准模板（套用后即删，合成可逆），写入对象仅限台账最新合成产品。

## 快速索引

| 模块 | 用例编号 | 用例标题 | 优先级 | 风险 |
| --- | --- | --- | --- | --- |
| 高级配置首页 | OP-PADV-001 | 卡片列表与文档链接 | P0 | 低 |
| 场景联动配置 | OP-PADV-002 | 场景联动页结构 | P1 | 低 |
| 消息推送配置 | OP-PADV-003 | 消息推送页结构 | P1 | 低 |
| 消息推送配置 | OP-PADV-004 | 套用模板与删除（写入） | P1 | 高 |
| 消息推送配置 | OP-PADV-005 | 套用模板弹窗结构与空选拦截 | P2 | 低 |
| 子页直达导航 | OP-PADV-006 | 子页 URL 直达与面包屑 | P2 | 低 |

## 模块：高级配置首页

<details>
<summary>OP-PADV-001｜卡片列表与文档链接｜P0｜低风险</summary>

> 前置条件：已登录；台账最新产品存在（当前为 2026-09-10 创建：自动化测试产品1789026290672 / 型号 at0672 / model at0635.light.at0672；本包不创建产品）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 从产品管理列表进入台账最新产品高级配置页 | 台账最新产品（at0672） | URL 含 /advanced，面包屑「高级配置」可见 |
| — | 2 | 观察文档提示 | 无 | 「设备开发和高级配置都与产品功能定义有关」与「点我查看」链接可见 |
| — | 3 | 点击「点我查看」 | 无 | 新标签打开文档中心页（URL 含 /service-support/home 且带 product-development 功能定义文档查询参数），原页不受影响；关闭新标签返回 |
| — | 4 | 观察卡片列表 | 无 | 「场景联动配置」「消息推送配置」两张卡片，标题与描述文案可见，状态徽标「可选」，卡片入口箭头在位 |

</details>

## 模块：场景联动配置

<details>
<summary>OP-PADV-002｜场景联动页结构｜P1｜低风险</summary>

> 前置条件：已登录，位于高级配置首页（台账最新产品 at0672）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点击「场景联动配置」卡片 | 无 | 面包屑出现「高级配置/场景联动配置」，页头标题与描述可见（子页 chunk 首访冷编译约 9s，脚本以就绪文本硬等） |
| — | 2 | 观察触发/执行两区 | 无 | 「触发条件设置」「执行动作设置」区与「新建自动化」按钮（触发区右上）可见；两表列头齐全（自动化名称、触发属性/事件（执行属性/方法）、样式类型、触发范围（执行范围）、自动化类型、状态、操作） |
| — | 3 | 观察列表状态 | 无 | 表格空态或数据行均可（行数注解；「删除」仅对 开发中且自定义 行开放注解；validFlag=0 失败行红底+警示图标如出现则注解） |
| — | 4 | 观察图例区 | 无 | 「金云智居App自动化场景图例」标题、图例图片与三步说明可见 |
| — | 5 | 观察页脚按钮组 | 无 | 「返回上一级」与「申请上线」按钮可见（申请上线不点击，提交流程范围外） |
| — | 6 | 点击页脚「返回上一级」 | 无 | 回到高级配置卡片列表（两卡片可见） |
| — | 7 | 再次进入场景联动页并点面包屑「高级配置」 | 无 | 面包屑逐级返回，回到卡片列表 |

</details>

## 模块：消息推送配置

<details>
<summary>OP-PADV-003｜消息推送页结构｜P1｜低风险</summary>

> 前置条件：已登录，位于高级配置首页（台账最新产品 at0672）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点击「消息推送配置」卡片 | 无 | 面包屑出现「高级配置/消息推送配置」，页头标题与描述可见 |
| — | 2 | 观察操作区 | 无 | 「套用模板」「新建推送」按钮可见可点 |
| — | 3 | 观察列表 | 无 | 表头齐全（消息推送标题/消息推送内容/触发条件/推送方式/审核状态/推送间隔/推送状态/操作）；空态或数据行注解（行内 查看/编辑/删除 与推送状态开关的可用性随审核状态变化：开关仅审核成功可开、编辑审核中禁用，源码规则注解） |
| — | 4 | 点击页脚「返回上一级」 | 无 | 回到高级配置卡片列表（两卡片可见） |
| — | 5 | 再次进入消息推送页并点面包屑「高级配置」 | 无 | 面包屑逐级返回，回到卡片列表 |

</details>

<details>
<summary>OP-PADV-004｜套用模板与删除（写入）｜P1｜高风险</summary>

> 前置条件：已登录，位于消息推送配置页（台账最新产品 at0672）；产品功能定义已选标准模板（模板按功能自动匹配）；**环境存在在线标准消息模板**（2026-09-03 实证：test 环境灯品类模板列表返回 totalCount=0，环境数据缺失时本用例注解转跳 skip，不判失败；待确认：环境在线标准模板数据）。写入对象仅限 create-product 台账最新合成产品（本轮 at0672，2026-09-10 创建），禁止触碰其他产品；套用后立即删除（合成可逆）。
>
> 是否写入数据：是（applyFunctionMessageTpl 套用 1 条标准消息模板 → 列表新增行；取消删除分支无写入；随后确认删除该行。新增/删除均经 src/support/recordGeneratedData.ts 记入本包 `runtime/generated-data.json` 台账，kind=open-platform-message-tpl / open-platform-message-tpl-delete，只增不删）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D01 | 1 | 点击「套用模板」勾选首个模板并确定 | `D01` 平台标准模板（按功能自动匹配，勾选≥1 条非空取值） | 弹窗出现模板列表；若列表为空（环境无在线标准模板）→ 注解转跳；否则勾选第一行后确定（请求期确定按钮 loading 禁用），toast「新增成功」，列表出现该模板行（标题与所选一致） |
| — | 2 | 台账记录（新增） | — | 本包 runtime/generated-data.json 追加模板新增记录（kind=open-platform-message-tpl） |
| — | 3 | 点击该行「删除」，确认弹窗中点「取消」 | 无 | 确认弹窗出现（「确认删除模板消息 {标题}」）；取消后弹窗关闭、行保留、无「删除成功」提示，列表行数不变 |
| — | 4 | 再次点击该行「删除」并确定 | 无 | 弹窗关闭，行消失，toast「删除成功」 |
| — | 5 | 台账记录（删除） | — | 本包台账追加模板删除记录（kind=open-platform-message-tpl-delete） |

</details>

<details>
<summary>OP-PADV-005｜套用模板弹窗结构与空选拦截｜P2｜低风险</summary>

> 前置条件：已登录，位于消息推送配置页（台账最新产品 at0672）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 打开「套用模板」弹窗观察结构 | 无 | 弹窗标题「套用消息推送模板」，提示「选用标准消息,无需审核即可上线;根据功能定义已自动匹配以下标准消息。」，表头 消息文案/触发条件/推送规则 可见（列表为空时为空态，不阻塞结构断言） |
| D02 | 2 | 不勾选直接确定 | `D02` 空勾选集合 | 提示「请选择至少一个模板」，弹窗不关闭，列表无新增 |
| — | 3 | 取消关闭 | 无 | 弹窗关闭，列表无变化 |

</details>

## 模块：子页直达导航

<details>
<summary>OP-PADV-006｜子页 URL 直达与面包屑｜P2｜低风险</summary>

> 前置条件：已登录，位于产品管理列表；台账最新产品存在（at0672）。2026-09-11 对象矩阵核对新增（绕过卡片入口的直达路径行：源码 watchEffect 按路径段解析激活卡片，子页路由 /advanced/scene、/advanced/message 独立注册）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 从列表进入台账最新产品基础配置页读取产品 id | 台账最新产品（at0672） | URL 形如 /integration/product/{id}/basic，{id} 可读取 |
| — | 2 | 直达 /integration/product/{id}/advanced/scene | 无 | 不经卡片点击直接渲染场景联动页：面包屑「高级配置/场景联动配置」，「触发条件设置」区可见 |
| — | 3 | 直达 /integration/product/{id}/advanced/message | 无 | 不经卡片点击直接渲染消息推送页：面包屑「高级配置/消息推送配置」，「套用模板」按钮可见 |

</details>
