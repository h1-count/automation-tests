# 开放平台高级配置：测试结论

## 当前结论（2026-09-03 首轮建立）

- 用例集 5 条（写入 1 条），4/5 通过 + 1 转跳（2026-09-03）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-03 探索与执行实证（at0635.light.at8438）：
  - 高级配置首页：两张卡片（场景联动配置/消息推送配置，均「可选」徽标）+「点我查看」文档链接（el-link @click 新窗口，无 href，a11y 树中为文本节点）。
  - 场景联动页：触发条件设置/执行动作设置两表结构齐全（自动化名称/触发属性事件/样式类型/触发范围/自动化类型/状态/操作），「新建自动化」按钮在位，金云智居App图例区完整；面包屑「高级配置」可返回卡片列表。
  - 消息推送页：「套用模板」「新建推送」按钮在位，表 8 列齐全，当前列表空态。
  - 套用模板弹窗：alert 提示「选用标准消息,无需审核即可上线…」+ 模板表格（选择所有行/消息文案/触发条件/推送规则）+ 空选拦截「请选择至少一个模板」全部实证。

## 关键环境事实（已回写用例表）

1. **test 环境灯品类无在线标准消息模板**：`/product/message/tpl/list`（productId=2095414624647254016，productTypeId=1189807104387571714，onlineStatus=1）返回 `totalCount=0`。OP-PADV-004（套用模板写入）按用例表约定注解转跳 skip，待环境补充模板后重跑；这不是页面缺陷（请求参数正确、空态渲染正常）。
2. 高级配置子页（scene/message）为懒加载 chunk，dev server 首访冷编译约 9s——定位策略：点击卡片后等待「就绪文本」可见（45s），不使用固定睡眠。

## 写入与数据说明

- 本包首轮**无台账记录**（唯一写入用例 OP-PADV-004 因环境模板数据缺失转跳）。台账 helpers（recordGeneratedMessageTpl / recordGeneratedMessageTplDelete）已就位，模板补充后重跑即记录（kind=open-platform-message-tpl / -delete，套用后即删合成可逆）。

## 后续关注

- 「新建自动化」完整表单（931 行，数值型样式比较符/范围整除跨字段校验）范围外，建议单独设计用例集。
- 「新建推送」完整表单（672 行模板编辑器，含触发条件编辑器）范围外。
- 推送状态开关（依赖审核成功模板）、申请上线（全局提交动作）范围外。

## 2026-09-14 复测（product-edit-chain-20260910，product-advanced）

- 四轮执行历程、全链路基准（R3 全绿）、状态推进说明与已知差异见 `testpacks/web/open-platform/test-reports/product-edit-chain-20260910.md`「执行历程与多轮说明」。
- 本包涉及的最新页面事实与脚本修订均已在 R3 全链路验证通过；R4 的失败为 at0672 送审态（R3 链路推进的业务结果）下的只读锁定，非本包用例/脚本缺陷。

### 2026-09-14 复测收官（R5–R7）
- R7：5 通过 + 1 设计内 skip（004 与 R3 基准一致）。无新增差异。
