# 开放平台企业中心：测试结论

## 当前结论（2026-09-03 首轮建立）

- 用例集 10 条（写入 3 条），全部可执行（无 fixme）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-03 探索实证（只读探索，快照与截图见 `runtime/exploration/out/`）：
  - 企业信息页（完整权限企业）：企业名称 自动化测试企业1788160280635、标识 at0635、ID 2094324398378061824、邮箱 autotest…@example.invalid、地址 自动化测试地址；产品接入/运营/服务三系统均「已开通」。
  - 受限企业联动：切至 自动化测试企业1788231909019b（标识 b9019）后仅产品接入系统「已开通」，运营/服务「未开通」；与 console-home 011 的入口收窄互证。探索结束后已切回完整权限企业。
  - 成员管理页（管理员视角）：表头 成员ID/手机号/成员姓名/备注/成员权限/操作 六列；管理员行（当前账号 239）操作列「-」、无勾选框；手机号列**脱敏显示 130\*\*\*\*0000**；「添加成员」「批量删除成员」「查询」「重置」入口齐全；侧边菜单组名为「企业管理」（企业信息/成员管理）。
  - 搜索：关键字命中当前账号；注意查询由「查询」按钮触发（探索中发现回车不触发查询——用例操作已改为点击查询按钮）。
  - 添加成员弹窗：空提交提示「请输入姓名」「请输入手机号」「请至少选择一项权限。」；手机号输入剔除非数字；备注 64 字截断；权限树默认全展开：iKinglink AIoT平台 → 产品接入系统（产品开发/授权码管理/采购管理/协议管理）、产品运营系统（设备管理/设备OTA/设备日志/数据概览）等。
- 侧边导航「UI组件示例<开发>」开发演示菜单组在企业页同样可见（与 account-center 包同一发现，跨页面均可见，缺陷候选升级：全站导航问题）。

## 写入说明（本包 3 个写入用例，全自动无人工介入）

1. OP-ENT-006：在受限企业添加成员 M1（自动化成员M1 / 13000000002 / 权限：产品开发），成功后记入本包 `runtime/generated-data.json`（runId=1）。
2. OP-ENT-008：删除 M1 出受限企业（二次确认「确定删除该成员吗？」），台账同 runId 覆盖追加 deletedAt（只增不删）。
3. OP-ENT-010：同手机号重新添加，实证需求「成员已有账号→不新建账号、开通企业权限」分支，台账追加 readdedAt。

**执行顺序约束**：006 → 007 → 008 → 010（010 依赖台账 deletedAt 标记）。

**结束态**：M1 为受限企业成员（权限：产品开发）——供补测轮以成员身份登录执行 account-center OP-ACCT-008（成员行退出企业）与 console-home OP-CONS-010（未加入企业空态，需先在受限企业删除 M1 或用未加入企业的视角账号）。

## 实现与需求的已知差异（探索实证）

1. 成员搜索仅「查询」按钮触发（回车不触发）——轻微交互问题，建议确认预期。
2. 权限树必选（「请至少选择一项权限。」）——无法经 UI 构造零权限成员，OP-CONS-010 走「未加入企业」路径。
3. 「UI组件示例<开发>」开发演示菜单组全站可见（缺陷候选，建议确认测试环境导航配置）。

## 待办（补测轮，依赖 M1 成员账号）

- account-center 包「成员行退出企业入口与二次确认」用例转正：以 M1 登录（短信登录人工协助点选图形验证码），核对所属企业成员行「退出企业」入口与二次确认弹窗（不真实退出）。
- console-home 包「未加入企业空态」用例转正：以 M1 登录后先经企业切换/退出流程构造「未加入企业」状态（或在受限企业删除 M1 后登录），实证空态文案「您没还有加入任何企业，请向企业管理员申请。」（错别字文案实证）。
