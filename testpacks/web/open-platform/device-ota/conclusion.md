# 开放平台固件升级：测试结论

## 当前结论（2026-09-04 首轮建立）

- 用例集 4 条（全 no_write——OTA/固件域按硬约束只做只读与表单校验），4/4 全部通过（2026-09-04，含一轮事实修正：包大小断言由独立元素改为文本包含「- MB」）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-04 实证：
  - 页面结构完整：标题「固件升级」+ 远程在线升级描述 + 产品卡片（名称/开发方式/Model/设备类型/通讯方式 + 切换图标）+ 表头 9 列齐全。
  - **切换产品弹窗**：关键字过滤生效（「1788420748438」→ 仅剩匹配项）；选择 7426 产品卡片联动、OTA 列表按产品刷新；切回 8438 恢复。产品列表仅非云云接入（实证 3 个台账 direct 产品）。
  - **新增固件升级表单校验**：D01 空表单确定 → 「请上传安装包」+「请输入固件版本号」双字段拦截、弹窗不关闭；产品名称/模型编码回显 disabled、包大小「- MB」；D02 版本号「abc」→ 字段级格式提示；D03 恢复 9.9.9 → 错误清空（升级包仍缺不提交）；取消关闭无新增，全程无上传、无提交请求。
  - 状态 tag 语义核对：既有行「待生效」= danger；操作列「下发」控件存在（**按硬约束不点击**——刷固件类高风险动作）。

## 实现差异与疑似缺陷

1. **既有 1 行升级配置来源不明**：自动化测试产品1788420748438 / at0635.light.at8438 / *.bin / 1.0.1 / 待生效 / 2026-09-03 16:42:34——**非本工程台账创建**（疑手工或历史数据），已保持只读未做任何变更，**建议向产品/运营确认来源与清理策略**。
2. 既有行「升级设备占比」为 0——与台账产品无绑定设备一致，数值合理。

## 写入与数据说明

- 本包**无台账记录**（全 no_write；表单校验仅触达客户端拦截，未提交任何请求）。

## 范围外（硬约束）与后续关注

- **新增固件升级写入**（上传固件+创建配置）与**下发/取消下发**（对设备推送固件=刷固件类高风险动作，AGENTS.md 硬边界第 2 条禁止）：待产品明确测试策略与专用测试设备后另行评估。
- 若删除该既有行/新增配置成为测试需求，需先走台账流程并确认设备影响面。
