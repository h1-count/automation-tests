# 开放平台设备调试：测试结论

## 当前结论（2026-09-03 首轮建立）

- 用例集 2 条（全 no_write），2/2 全部通过（2026-09-03）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-03 实证：
  - **设备调试入口已下线**：`src/data/menus/integration.ts` 第 10 行菜单项被注释；产品接入侧边栏无「设备调试」（授权码管理/采购管理/协议管理在列）。
  - 直达 `/integration/debugging`：路由存在（复用 `authcode/AuthCode.vue`）但未配置权限 → BaseLayout 渲染「您未被授权访问此页面」空态，面包屑/i18n 名为「接口调试」，URL 不跳转登录。
  - 同组件差异核对：授权码页正常渲染（已授权）⇄ debugging 被权限拦截——差异仅在权限配置，组件同源。

## 查重结论

- debugging 路由与授权码页共用 AuthCode.vue，**页面功能级覆盖以 authcode 包为准**（5/5 通过，见 authcode/conclusion.md）；本包仅覆盖入口缺失与权限拦截的行为面。

## 写入与数据说明

- 本包**无台账记录**（全 no_write）。

## 待确认（产品侧）

1. 设备调试功能是否已废弃（菜单注释 + 权限未配置的双重下线信号）？若恢复发布：去掉菜单注释并配置路由权限后，页面内容即授权码页（同组件），可直接复用 authcode 包用例，另补「接口调试」命名与入口可达性用例。

## 后续关注

- 若权限配置由后端权限码下发变化（账号被授予 /integration/debugging 路径），直达将渲染授权码页——本包 OP-DBG-001/002 届时需按新事实复核。
