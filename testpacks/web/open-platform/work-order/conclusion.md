# 开放平台技术工单：测试结论

## 当前结论（2026-09-04 首轮建立）

- 用例集 4 条（OP-WO-004 为已确认写入用例），4/4 全部通过（2026-09-04，含两轮事实修正：导航标题以页面文本断言；幂等段先等列表加载完成再统计+台账自愈认领）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-04 实证：
  - 列表：左侧导航「技术工单」+ 5 统计卡（待处理/处理中/待验收/已完成/已撤销）+ 全部状态筛选 + 新建工单按钮 + 表头 7 列齐全。
  - 新建向导：3 步结构（选择工单所属系统/选择工单类别/请填写工单信息）+ 回复时效提示（5-7 个工作日）；产品接入系统类别联动 13 类齐全；产品服务/产品运营系统类别已注解。
  - 表单校验：**创建按钮 disabled 门槛实证**（system+category+title+description 全齐才 enabled）；标题 maxlength=20 截断、描述 maxlength=200 截断；取消返回无新增。
  - **写入用例 OP-WO-004**：创建合成工单「自动化测试工单1788487750409」（编号 2095695640280567808，产品接入系统/其他/等级低）成功；台账已记录（kind=open-platform-work-order）。重跑走幂等分支（列表已有合成工单 → 跳过创建）。
  - 幂等段加固：台账解析兼容 `{records:[...]}` 结构；数行前等待「数据行或空态」出现；列表已有前缀工单时逐行核对台账、缺失自愈补记（只增不删）。

## 实现差异与疑似缺陷

1. 第二次尝试（runId 1788487938808）在提交后未产生列表行且台账无记录——以列表与台账双源核对，该次**未实际创建**（疑提交被后端拒绝或未发出），无孤儿数据残留；幂等加固后重复执行不再产生该不确定性。
2. 「发放状态」列语义（列表列名）与工单场景的常规「状态」表述不一致——源码即如此，注解留档。

## 写入与数据说明

- 台账：`testpacks/web/open-platform/work-order/runtime/generated-data.json`，1 条记录（runId 1788487750409，ticketNumber 2095695640280567808）。
- 工单无 UI 删除路径，台账只增不删；后续重跑自动幂等跳过。

## 后续关注

- 工单进入支持处理流程后：补状态流转（待处理→处理中→待验收→已完成）、回复与验收交互用例。
- 撤销工单入口出现后补撤销用例（需同步台账状态注解）。
