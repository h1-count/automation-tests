# 用例集：开放平台协议管理（cloud-bridge）

> 测试类型：Web ｜ 默认环境：test ｜ 默认数据策略：混合——OP-CBP-001~004 no_write；OP-CBP-005~009 写入（数据全合成、经台账记录，删除仅限本工程台账创建的合成协议）
>
> 上下文来源：`sources/open-platform/需求/产品接入系统功能需求说明/产品接入系统功能需求说明.md`（「云云接入/协议管理」章节）、前端源码 `web-open-platform/src/pages/integration/cloud-bridge-protocol/CloudBridgeProtocol.vue`（列表 177 行：状态开关 before-change 写接口 + 删除 confirm）、`components/BtnEditCloudProtocol.vue`（新增/编辑协议表单 187 行：创建前强制 queryCloudAccessService，applicationId 为空报错不创建）、`CloudBridgeProtocolDetail.vue`（三步配置页 439 行：StepProgress 协议信息/上行解析/下行解析）、`CloudBridgeProtocolDetailS1/S2/S3.vue`（各步骤区块）、`api/bridge/bridge-basic.ts`、`api/integration/develop-cloud.ts`（queryCloudAccessService / openCloudAccessService）、**2026-09-07 新增勘察：`pages/integration/product/components/BtnOpenCloudService.vue`（开通云云服务自助弹窗）、`DevelopMethodCloud.vue`（集成开发页开通步骤卡）、`ProductPhaseDevelop.vue`、`src/routes/integration.ts`（`/integration/product/:id/develop`）**、`experience/general.md`、`experience/open-platform.md`；查重比对 authcode（debugging 路由复用其组件，彼包处理）。
>
> 上下文结论：①协议管理页：标题「协议管理」+ 描述（配置云云接入协议转换Bridge）+ 搜索区 +「新增协议」按钮 + 表格（序号/协议名称/协议描述/是否关联产品[已关联 success｜未关联 info]/状态 switch 启用|禁用/创建时间/操作[编辑|删除]；「查看」入口已被注释）；2026-09-03 实证列表 0 行；②新增协议弹窗：标题「新增协议」，字段 协议规则名称（required，maxlength=32，placeholder 请输入协议规则名称）+ 协议描述（textarea，maxlength=256，rows=5）；空表单确定 → 「请输入协议名称」拦截（探针实证）；③创建链路此前被业务状态阻塞（2026-09-03 实证 openStatus=0、applicationId=null，以 OP-CBP-004 错误路径覆盖）；④协议创建成功后会授权并跳转三步配置页（编辑路由 :id/edit）；⑤**2026-09-07 勘察+实证：云云接入服务为自助开通，入口在云云设备产品创建流程的「设备开发」步骤页**（`/integration/product/:id/develop`，五步流程第三步；仅 开发方式=云云接入 且 协议类型=自定义协议 的产品渲染该卡片；开发方式=开放协议接入 的产品该页为 MQTT 文档/授权码/固件，无开通入口）——「云云接入服务集成开发」步骤卡「开通服务」按钮；弹窗字段 项目名称固定「云云接入服务」（disabled）、数据中心（字典 AIOT_DATA_CENTER，异步加载需打开下拉选择）、联系人/联系方式（预填当前账号 realName/phoneNumber，required）；提交 `POST /open/internal/open/cloud/device/access/service` → toast「云云接入服务开通成功」→ openStatus=1 + applicationId；已开通后按钮变「查看服务」（幂等防重）；⑥删除为 confirm 危险确认，状态启停走 before-change 写接口。
>
> 范围外声明：**三步配置页的完整配置面**（Step1 协议信息/产品-协议映射/公共Header、Step2 上行认证+上行解析规则+数据转换预览、Step3 下行认证+下行规则+规则测试——组件面约 1.3 万行，按同标准另行独立成包；本包 OP-CBP-007 仅覆盖可达与结构冒烟）；**协议名称唯一性/重复创建**（源码未见唯一性校验入口，刻意制造重复合成协议会污染列表——见 scope.json C03 范围外理由）；**「查看」入口**（源码已注释，功能不存在）。
>
> 本表供测试范围审核。真实密码、Token、密钥、Cookie 等可复用凭据仅从本地 `.env` 或对应登录态存储读取；非法值均为合成构造（D01~D05）；写入链路数据全部为合成值并经台账记录，删除仅限台账创建的合成协议。

## 快速索引

| 模块 | 用例编号 | 用例标题 | 优先级 | 风险 |
| --- | --- | --- | --- | --- |
| 协议列表 | OP-CBP-001 | 协议管理页渲染 | P0 | 低 |
| 协议列表 | OP-CBP-002 | 搜索筛选交互 | P2 | 低 |
| 新增协议 | OP-CBP-003 | 弹窗结构与字段校验（边界） | P1 | 中 |
| 新增协议 | OP-CBP-004 | 云云服务未开通提交拦截（错误路径） | P1 | 中 |
| 云云服务开通 | OP-CBP-005 | 开通云云接入服务（写入） | P0 | 中 |
| 新增协议 | OP-CBP-006 | 创建协议写入链路（写入） | P0 | 中 |
| 三步配置页 | OP-CBP-007 | 三步配置页可达与结构冒烟 | P1 | 低 |
| 协议列表 | OP-CBP-008 | 编辑回显与状态启停（写入） | P1 | 中 |
| 协议列表 | OP-CBP-009 | 删除合成协议自清理（写入） | P1 | 中 |
| 三步配置页-Step1 | OP-CBP-010 | 产品-协议映射添加与删除（写入） | P1 | 中 |
| 三步配置页-Step1 | OP-CBP-011 | 公共Header新增与删除（写入） | P1 | 中 |
| 三步配置页-Step2 | OP-CBP-012 | 上行认证配置双方式查看与保存（写入） | P1 | 中 |
| 三步配置页-Step2 | OP-CBP-013 | 上行解析规则列表初始化与启停（写入） | P1 | 中 |
| 三步配置页-Step2 | OP-CBP-014 | 上行规则编辑抽屉结构冒烟（取消不落库） | P2 | 低 |
| 三步配置页-Step3 | OP-CBP-015 | 下行通讯认证配置保存（写入） | P1 | 中 |
| 三步配置页-Step3 | OP-CBP-016 | 下行规则添加与删除（写入） | P1 | 中 |
| 三步配置页-Step3 | OP-CBP-017 | 数据转换预览与测试转换+最终保存（写入） | P1 | 中 |
| 协议详情 | OP-CBP-018 | 详情页只读渲染与翻步 | P2 | 低 |
| 协议列表 | OP-CBP-019 | 协议状态启用/禁用状态流转（写入） | P0 | 中 |
| 新增协议 | OP-CBP-020 | 新增协议弹窗字段正反与极限值（no_write 深度校验） | P1 | 低 |
| 三步配置页-Step3 | OP-CBP-021 | 下行规则表单字段正反与边界（no_write 校验） | P1 | 低 |
| 三步配置页-Step2 | OP-CBP-022 | 上行规则表单字段正反与边界（no_write 校验） | P1 | 低 |
| 三步配置页-Step1 | OP-CBP-023 | 公共Header表单字段正反与边界（no_write 校验） | P1 | 低 |

## 模块：协议列表

<details>
<summary>OP-CBP-001｜协议管理页渲染｜P0｜低风险</summary>

> 前置条件：已登录。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 进入协议管理页（/integration/cloud-bridge-protocol） | 无 | 标题「协议管理」与描述（配置云云接入协议转换Bridge）可见 |
| — | 2 | 观察工具栏 | 无 | 「新增协议」按钮可见 |
| — | 3 | 观察列表 | 无 | 表头齐全（序号/协议名称/协议描述/是否关联产品/状态/创建时间/操作）；空态「暂无数据」（2026-09-03 实证 0 行） |

</details>

<details>
<summary>OP-CBP-002｜搜索筛选交互｜P2｜低风险</summary>

> 前置条件：已登录，位于协议管理页。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察搜索区字段 | 无 | 搜索区渲染；字段清单注解（2026-09-03 实证：本页 search-form 为 slim 模式且无筛选字段，仅承载新增按钮插槽） |
| — | 2 | 存在筛选字段时输入过滤（否则注解跳过） | 合成值「自动化测试协议」 | 有字段则输入生效、无报错；无字段注解实证并跳过 |

</details>

<details>
<summary>OP-CBP-008｜编辑回显与状态启停（写入）｜P1｜中风险</summary>

> 前置条件：已登录，位于协议管理页；OP-CBP-006 已创建台账合成协议且未删除（候选 = 台账最新协议且当前列表可见）。
>
> 是否写入数据：是（编辑保存 + 状态启停 before-change 写接口；仅作用于本工程台账创建的合成协议）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D04 | 1 | 点击台账协议行操作列「编辑」（跳转 :id/edit 三步页，2026-09-07 实证：列表行「编辑」为路由链接而非弹窗） | 台账最新协议 | 三步页打开且回显：协议规则名称=D04 值、协议描述=D05 值（Step1 表单） |
| D06 | 2 | 修改协议描述后点「保存信息配置」 | D06 描述=合成值「自动化测试协议描述修订<runId尾6位>」 | 保存成功（无错误提示）；刷新后回显 D06 值 |
| — | 3 | 返回协议管理页，点击状态开关（启用↔禁用） | 无 | 无确认弹窗：before-change 直接调写接口，成功后开关翻转，列表状态列更新 |
| — | 4 | 再次切换恢复原状态 | 无 | 状态恢复，台账记录两次切换时间戳 |

</details>

<details>
<summary>OP-CBP-009｜删除合成协议自清理（写入）｜P1｜中风险</summary>

> 前置条件：已登录，位于协议管理页；OP-CBP-006 已创建台账合成协议（本用例删除该协议完成自清理）。
>
> 是否写入数据：是（仅删除本工程台账创建的合成协议；删除前完成 006~008 的验证）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D04 | 1 | 点击台账协议行操作列「删除」 | 台账最新协议（名称核对） | 出现 confirm 确认弹窗（危险确认文案可见） |
| — | 2 | 确认删除 | 无 | 成功提示，该行从列表消失 |
| — | 3 | 台账记录 | — | 本包 runtime/generated-data.json 追加协议记录的 deletedAt 标记（台账只增不删） |

</details>

## 模块：新增协议

<details>
<summary>OP-CBP-003｜弹窗结构与字段校验（边界）｜P1｜中风险</summary>

> 前置条件：已登录，位于协议管理页。2026-09-03 实证：协议规则名称 required（提示「请输入协议名称」）、maxlength=32；协议描述 maxlength=256。
>
> 是否写入数据：否（逐字段喂非法值断言字段级提示，不点击提交；客户端拦截例外点击确定断言拦截）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 打开「新增协议」弹窗 | 无 | 弹窗标题「新增协议」；协议规则名称 + 协议描述（textarea 5 行）字段可见 |
| D01 | 2 | 空表单点击确定 | 无 | 提交被拦截：字段级提示（请输入协议名称），弹窗不关闭 |
| D02 | 3 | 协议规则名称输入 33 个字符 | 合成值「协」×33 | maxlength=32 截断为 32 字（实证注解） |
| D03 | 4 | 恢复合法值 | 名称=合成值；描述=合成值 | 字段错误清空（不实际提交——提交链路见 OP-CBP-004/006） |
| — | 5 | 取消关闭 | 无 | 弹窗关闭，列表无新增 |

</details>

<details>
<summary>OP-CBP-004｜云云服务未开通提交拦截（错误路径）｜P1｜中风险</summary>

> 前置条件：已登录，位于新增协议弹窗；已监听网络请求（排除只读查询）。2026-09-03 只读探针实证：queryCloudAccessService 返回 openStatus=0、applicationId=null（账号未开通云云接入服务）。**若服务已开通（含本包 OP-CBP-005 开通后重跑），本用例预期自然失效：改为注解记录当前 openStatus 并跳过拦截断言（前置状态不满足时的自适应分支）。**
>
> 是否写入数据：否（服务前置检查失败，无创建请求发出；仅只读查询）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 填合法名称与描述后点击确定 | 名称=合成值「自动化测试协议校验」；描述=合成值 | 未开通：出现错误提示（无云云接入服务信息——语义一致即通过），弹窗不关闭；已开通：注解记录状态并跳过本断言 |
| — | 2 | 网络监听核对 | 无 | 未开通分支：无协议创建请求（create/authorization）发出；仅只读查询请求 |

</details>

<details>
<summary>OP-CBP-006｜创建协议写入链路（写入）｜P0｜中风险</summary>

> 前置条件：已登录，位于协议管理页；云云接入服务已开通（OP-CBP-005 完成，openStatus=1、applicationId 非空）。
>
> 是否写入数据：是（创建合成协议并经台账记录；创建成功触发授权跳转三步配置页）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D04 | 1 | 打开「新增协议」弹窗，填名称与描述后确定 | D04 名称=合成值「自动化测试协议CBP<runId尾6位>」（≤32）；D05 描述=合成值「自动化测试合成协议-云云桥接<runId尾6位>」 | 无错误提示；列表出现该协议行（未关联=info、状态=启用），台账记录协议数据 |
| — | 2 | 观察创建后跳转 | 无 | 自动跳转三步配置页（:id/edit 路由，见 OP-CBP-007） |
| — | 3 | 台账记录 | — | 本包 runtime/generated-data.json 记录协议名称/描述/配置页路由（含协议 id） |

</details>

## 模块：云云服务开通

<details>
<summary>OP-CBP-005｜开通云云接入服务（写入）｜P0｜中风险</summary>

> 前置条件：已登录。前置写入：通过创建向导创建「云云接入+自定义协议」合成产品（照明-灯/WiFi/普通设备，入台账）——开通卡片仅在该类产品的设备开发步骤页渲染。幂等分支：若服务已开通（按钮为「查看服务」），直接验证服务状态并注解跳过提交。
>
> 是否写入数据：是（开通云云接入服务：产生服务绑定 openStatus=1 + applicationId，account 级一次性动作；表单数据为合成/预填值）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D08 | 1 | 创建向导：选品类（照明-灯）→ 方案「云云接入」→ 填名称/型号 → 协议类型选「自定义协议」（云云方案独有必填项）→ 通讯协议 WiFi → 设备类型 普通设备 → 创建 | D08 产品名=合成值「自动化测试云云产品<runId尾6位>」；型号=at<runId尾6位> | toast「产品创建成功」；跳转基础配置页；该产品记入台账 |
| D08 | 2 | 进入该产品设备开发步骤页（:id/develop），观察开通入口状态 | 无 | 未开通：「开通服务」按钮可见；已开通：「查看服务」+ 服务状态「已开通」（幂等分支：注解记录后用例通过并跳过 3~5） |
| — | 3 | 打开「开通服务」弹窗，选择数据中心第一项 | 无 | 弹窗标题「开通云云服务」；项目名称固定「云云接入服务」（disabled）；数据中心下拉选择第一项；联系人/联系方式预填当前账号信息 |
| D07 | 4 | 确认开通 | D07 联系方式=账号预填合成手机号（不改预填值） | toast「云云接入服务开通成功」；步骤卡状态刷新 |
| — | 5 | 验证开通结果 | 无 | 按钮变「查看服务」；服务状态「已开通」；台账记录开通时间与数据中心 |
| — | 6 | 网络监听核对 | 无 | 开通请求为 `POST /open/internal/open/cloud/device/access/service` 且仅发出一次 |

</details>

## 模块：三步配置页

<details>
<summary>OP-CBP-007｜三步配置页可达与结构冒烟｜P1｜低风险</summary>

> 前置条件：已登录；OP-CBP-006 创建协议成功并跳转三步配置页（:id/edit）。
>
> 是否写入数据：否（仅观察页面结构，不提交任何配置——完整配置面由 OP-CBP-010~018 覆盖）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察步骤条 | 无 | StepProgress 三步可见：协议信息/上行解析/下行解析 |
| — | 2 | 观察 Step1（协议信息）区块 | 无 | 协议信息表单区块渲染（以源码 S1 组件结构为准，具体字段注解记录） |
| — | 3 | 返回协议管理页 | 无 | 台账协议行存在于列表，本包冒烟结束 |

</details>


## 模块：三步配置页-Step1（协议信息）

<details>
<summary>OP-CBP-010｜产品-协议映射添加与删除（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（取 OP-CBP-006 产出，进入其 :id/edit Step1）。
>
> 是否写入数据：是（协议的产品-协议映射 1 条增/1 条删；作用于台账合成协议；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | Step1 点击「添加映射关系」 | 无 | 映射弹窗打开，含产品选择控件 |
| D08 | 2 | 选择云云前置产品并确认 | 台账云云产品（自动化测试云云产品<runId尾6位>） | 映射列表出现该产品行（产品名称/model 列非空） |
| — | 3 | 删除该映射行 | 无 | 行消失（确认交互按实现注解记录） |

</details>

<details>
<summary>OP-CBP-011｜公共Header新增与删除（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（进入其 :id/edit Step1）。
>
> 是否写入数据：是（协议公共 Header 1 条增/1 条删；合成值；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D09 | 1 | 点击「新增 Header」填写参数并确定 | D09 Header 参数=合成值 bridge-hdr-<runId尾6位十进制转小写字母（2026-09-07 实证：参数名 pattern 仅允许 A-Z a-z _ -，不得含数字）>；值来源=静态值 | Header 列表出现该行 |
| — | 2 | 删除该 Header 行 | 无 | 行消失（确认交互按实现注解记录） |

</details>

## 模块：三步配置页-Step2（上行解析）

<details>
<summary>OP-CBP-012｜上行认证配置双方式查看与保存（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（进入其 :id/edit，query.step=2 或点下一步到 Step2）。
>
> 是否写入数据：是（协议上行认证配置保存 1 次；内容为切换后的认证方式；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察认证方式默认态 | 无 | 认证方式 radio 可见（平台标准认证方式/自定义认证方式） |
| — | 2 | 切到「平台标准认证方式」 | 无 | 平台标准认证信息区块出现（认证类型/项目ID/平台回调地址/App key/数据接收方式/App secret） |
| — | 3 | 切到「自定义认证方式」→ 上行认证方式选「Basic Auth」 | 无 | Basic Auth 配置区块出现（App key/App secret 类字段） |
| — | 4 | 点「保存认证配置」 | 无 | 保存成功提示出现（提示类断言按行为断言），无错误提示 |

</details>

<details>
<summary>OP-CBP-013｜上行解析规则列表初始化与启停（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step2）。
>
> 是否写入数据：是（上行规则状态启停切换 2 次，最终恢复原状态；作用于协议自动初始化的标准规则；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 进入 Step2 观察「上行解析规则列表」 | 无 | 列表渲染 ≥1 条标准规则（规则名称/触发条件/回调地址列非空；initBridgeRuleList 自动初始化） |
| — | 2 | 切换首行状态开关 | 无 | 开关翻转（aria-checked 变化，无确认弹窗） |
| — | 3 | 再次切换恢复原状态 | 无 | 状态恢复，台账记录两次切换时间戳 |

</details>

<details>
<summary>OP-CBP-014｜上行规则编辑抽屉结构冒烟（取消不落库）｜P2｜低风险</summary>

> 前置条件：已登录；OP-CBP-013 已见规则列表。
>
> 是否写入数据：否（抽屉只查看并取消关闭）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点首行「编辑」打开规则抽屉 | 无 | 抽屉打开，含 3 个 tab：1. 规则基础信息 / 2. 解析映射规则 / 3. 响应处理 |
| — | 2 | 逐个切换 tab 观察结构 | 无 | 各 tab 面板渲染（字段以源码为准注解记录）；第 3 tab 底部出现「完成配置」 |
| — | 3 | 取消/关闭抽屉 | 无 | 抽屉关闭；规则列表数据不变（行数与状态不变） |

</details>

## 模块：三步配置页-Step3（下行解析）

<details>
<summary>OP-CBP-015｜下行通讯认证配置保存（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）。
>
> 是否写入数据：是（协议下行认证配置保存 1 次；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 进入 Step3 观察「下行通讯认证配置」 | 无 | 认证配置表单渲染（DownAuthConfig 自定义认证字段） |
| D10 | 2 | 填写必填认证字段 | D10 合成值（字段名以实现为准，注解记录） | 字段填写成功 |
| — | 3 | 点「保存认证配置」 | 无 | 保存成功提示出现，无错误提示 |

</details>

<details>
<summary>OP-CBP-016｜下行规则添加与删除（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）。
>
> 是否写入数据：是（下行规则 1 条增/1 条删；合成值；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D11 | 1 | 点「添加下行规则」，tab1 填基础信息 | D11 规则名称=自动化测试下行规则<runId尾6位>；Control Type=首个可选项 | tab1 表单填写完成 |
| — | 2 | 逐 tab 下一步到末 tab 完成/保存 | 无 | 规则创建成功，下行规则列表出现该行 |
| — | 3 | 删除该规则行 | 无 | 确认后行消失（删除交互按实现注解记录） |

</details>

<details>
<summary>OP-CBP-017｜数据转换预览测试转换+最终保存（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）；OP-CBP-013 已初始化上行标准规则。
>
> 是否写入数据：是（handleFinalSave 最终保存协议配置 1 次；testTransform 为测试转换接口不产生业务数据；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | Step2 数据转换预览：选择测试模板样例（必选，实证：未选样例转换被拦截） | 无 | 样例选中，输入样例文本框就绪 |
| — | 2 | 点「测试转换」 | 无 | 「转换成功」与「输出三方数据」区渲染 |
| — | 3 | 翻到第 3 步点「保存」 | 无 | 保存成功提示出现；协议配置持久化（字段无残留错误） |

</details>

## 模块：协议详情

<details>
<summary>OP-CBP-018｜详情页只读渲染与翻步｜P2｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 打开 :id/detail | 无 | 三步结构渲染为只读态：S1 隐藏「保存信息配置/添加映射关系」及映射操作列（2026-09-07 实证修订：「新增 Header」按钮 View 态仍渲染——疑似实现缺口，按实证断言并注解） |
| — | 2 | 逐步下一步到第 3 步 | 无 | StepProgress 步进正常，S2/S3 只读渲染 |
| — | 3 | 返回列表 | 无 | 协议行仍存在，无数据变化 |

</details>


## 模块：协议状态流转

<details>
<summary>OP-CBP-019｜协议状态启用/禁用状态流转（写入）｜P0｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（OP-CBP-006 产出，OP-CBP-009 删除前执行）。
>
> 是否写入数据：是（协议状态启停切换 2 次并回读持久化，最终恢复原状态；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 记录初始状态并切换开关（启用↔禁用） | 无 | 开关 aria-checked 翻转，状态列文案同步变化（启用/禁用），无确认弹窗 |
| — | 2 | 刷新页面回读状态 | 无 | 切换后状态持久化（与刷新前一致，非仅前端态） |
| — | 3 | 再次切换恢复原状态并刷新复核 | 无 | 状态恢复初始值，台账记录切换与恢复时间戳 |

</details>

## 模块：字段正反与边界（深度校验）

<details>
<summary>OP-CBP-020｜新增协议弹窗字段正反与极限值（no_write 深度校验）｜P1｜低风险</summary>

> 前置条件：已登录，位于新增协议弹窗。客户端校验拦截场景：不产生任何网络写入。
>
> 是否写入数据：否（非法组合不点确定或提交被客户端拦截且无写请求）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D13 | 1 | 名称空 → 点确定 | 空 | 出现「请输入协议名称」字段级提示，无创建请求 |
| D13 | 2 | 名称 1 字符（下边界内） | 1 个合成字符 | 无字段级报错 |
| D13 | 3 | 名称 33 字符（超上界）观察截断 | 33 个合成字符 | 截断为 32 字符 |
| D13 | 4 | 描述 257 字符观察截断 | 257 个合成字符 | 截断为 256 字符 |
| D13 | 5 | 名称含 emoji/空格/符号混合 | 合成混合串 | 客户端接受或字段提示（行为注解记录） |
| — | 6 | 取消关闭弹窗 | 无 | 弹窗关闭，列表无新增 |

</details>

<details>
<summary>OP-CBP-021｜下行规则表单字段正反与边界（no_write 校验）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）；已打开「添加下行规则」抽屉 tab1。
>
> 是否写入数据：否（校验场景保存被客户端拦截且无写请求）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D14 | 1 | 规则名称空/超界 → 触发校验 | 空值；33 字符（规则 required 且 length(1,32)） | 出现字段级提示；33 字符被 maxlength 截断为 32 |
| D14 | 2 | 规则描述 257 字符观察截断 | 257 个合成字符 | 截断为 256 字符（length(1,256)） |
| D14 | 3 | Control Type 未选 → 保存 | 无 | required 拦截，出现字段级提示，无创建请求 |
| — | 4 | 取消关闭抽屉 | 无 | 抽屉关闭，下行规则列表无新增 |

</details>

<details>
<summary>OP-CBP-022｜上行规则表单字段正反与边界（no_write 校验）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step2）；已打开上行规则编辑抽屉 tab1。2026-09-07 实证修订：标准规则「规则名称」disabled 只读。
>
> 是否写入数据：否（校验场景取消关闭，不触发 updateCBPThirdRule）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D15 | 1 | 打开标准上行规则编辑抽屉观察「规则名称」 | 无 | 规则名称只读（disabled；2026-09-07 实证：标准规则名称不可编辑，负向校验转为回调地址） |
| D15 | 2 | 回调地址非法值 → 触发下一步/完成配置校验 | 非法路径合成串（不带前导 /） | 字段级提示（apiPath 自定义校验：须以 / 开头的合法 API 路径） |
| D15 | 3 | 消息类型标识符/值 65 字符观察截断 | 65 个合成字符 | 截断为 64 字符（maxlength=64） |
| — | 4 | 取消关闭抽屉 | 无 | 抽屉关闭，规则列表数据不变 |

</details>

<details>
<summary>OP-CBP-023｜公共Header表单字段正反与边界（no_write 校验）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step1）；已打开「新增 Header」弹窗。
>
> 是否写入数据：否（校验场景取消关闭，不触发 createProtocolHeader）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D16 | 1 | Header 参数空 → 确定 | 空 | 字段级提示（headerKey required，maxlength=32） |
| D16 | 2 | Header 参数 33 字符观察截断 | 33 个合成字符 | 截断为 32 字符 |
| D16 | 3 | 值来源=静态值 → 静态值留空 → 确定 | 值来源=静态值，值空 | 静态值必填提示或确定被拦截（以实现为准注解） |
| D16 | 4 | 值来源=平台生成 → 观察联动 | 无 | 出现类型/单位/格式联动字段（timestamp/random 等） |
| — | 5 | 取消关闭弹窗 | 无 | 弹窗关闭，Header 列表无新增 |

</details>

## 数据说明

- D01~D03、D04~D16 均为合成构造值；D04/D06/D09/D11/D13/D14/D15/D16 含 runId 尾 6 位或固定合成值防撞设计；写入数据经 `src/support/recordGeneratedData.ts` 记录到本包 `runtime/generated-data.json`（只增不删）。
- OP-CBP-005 开通动作依赖账号当前状态（一次性）；重跑时走幂等分支（已开通即验证通过）。
