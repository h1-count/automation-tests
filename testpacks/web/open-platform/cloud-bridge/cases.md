# 用例集：开放平台协议管理（cloud-bridge）

> 测试类型：Web ｜ 默认环境：test ｜ 默认数据策略：混合——no_write 勘察/校验用例与写入用例并存，各用例「是否写入数据」逐条声明；写入数据全合成、经台账记录（`src/support/recordGeneratedData.ts`），删除仅限本工程台账创建的合成协议/参数/规则。
>
> 上下文来源：`sources/open-platform/需求/产品接入系统功能需求说明/产品接入系统功能需求说明.md`（「云云接入/协议管理」章节）、前端源码 `web-open-platform/src/pages/integration/cloud-bridge-protocol/CloudBridgeProtocol.vue`（列表 177 行：状态开关 before-change 写接口 + 删除 confirm）、`components/BtnEditCloudProtocol.vue`（新增/编辑协议表单 187 行：创建前强制 queryCloudAccessService，applicationId 为空报错不创建）、`CloudBridgeProtocolDetail.vue`（三步配置页 439 行：StepProgress 协议信息/上行解析/下行解析）、`CloudBridgeProtocolDetailS1/S2/S3.vue`（各步骤区块）、`api/bridge/bridge-basic.ts`、`api/integration/develop-cloud.ts`（queryCloudAccessService / openCloudAccessService）、2026-09-07 勘察：`pages/integration/product/components/BtnOpenCloudService.vue`（开通云云服务自助弹窗）、`DevelopMethodCloud.vue`（集成开发页开通步骤卡）、`ProductPhaseDevelop.vue`、`src/routes/integration.ts`（`/integration/product/:id/develop`）、**2026-09-09 勘察：`sources/open-platform/需求/云云bridge优化/云云bridge优化.md`、`sources/open-platform/需求/云云bridgeBug/云云bridgeBug.md`（18 项验证清单）、`sources/open-platform/平台文档/云云接入bridge协议转换操作指南/云云接入bridge协议转换操作指南.md`（上行/下行数据解析配置章节）、前端源码 `components/BtnEditUpRule.vue`（抽屉标题=前缀+规则名，tab 头不可点击）、`BtnEditUpRuleFormBasic/FormTrigger.vue`、`BtnEditUpAnalysisRules.vue`（可视化映射+步进集中校验 validateAnalysisRules）、`UpDataParamsTable.vue`（只读参数表+行内编辑/删除+静默保存单条「添加成功」）、`ParamEditDialog.vue`（提取表达式 max64/数据类型/键值对模式/转换类型 4 项，枚举值映射规则与设备时间格式动态表单）、`BtnEditUpResponseProcessing.vue`（响应表达式配置/payload参数映射/平台标准参数表/第三方payload模板）、`PayloadParamsTable.vue`（payload参数表二级标题）、`PayloadParamEditDialog.vue`（值来源 平台生成/静态值 联动子配置；上行 tab3 与下行 body/query 共用）、`UpParsingRulesList.vue`、`UpAuthTypeBasicAuth.vue`（无 show-password，未被 S2 引用）、`BtnEditDownRule.vue`（下行抽屉标题动态「编辑{规则名}」）、`BtnEditDownRuleFormBasic.vue`（下行触发条件小标题）、`BtnEditDownRuleFormRules.vue`（指令数据/自定义Body模板按类型隐藏；Query 表 GET/DELETE、Body 表 POST/PUT）、`BtnEditDownRuleFormResponse.vue`（返回参数说明仅 GetToken/AddNode，参数说明 span 固定展示）、`DownRuleTest.vue`（输出面板「输出下行指令」）、`DownAuthTypeBasicAuth.vue`（密码 show-password）、`types/integration/cloud-enum.ts`、`experience/general.md`、`experience/open-platform.md`；查重比对 authcode（debugging 路由复用其组件，彼包处理）。
>
> 上下文结论：①协议管理页：标题「协议管理」+ 描述（配置云云接入协议转换Bridge）+ 搜索区 +「新增协议」按钮 + 表格（序号/协议名称/协议描述/是否关联产品[已关联 success｜未关联 info]/状态 switch 启用|禁用/创建时间/操作[编辑|删除]；「查看」入口已被注释）；②新增协议弹窗：标题「新增协议」，字段 协议规则名称（required，maxlength=32，placeholder 请输入协议规则名称）+ 协议描述（textarea，maxlength=256，rows=5）；空表单确定 → 「请输入协议名称」拦截（探针实证）；③创建链路此前被业务状态阻塞（2026-09-03 实证 openStatus=0、applicationId=null，以 OP-CBP-004 错误路径覆盖）；④协议创建成功后会授权并跳转三步配置页（编辑路由 :id/edit）；⑤2026-09-07 勘察+实证：云云接入服务为自助开通，入口在云云设备产品创建流程的「设备开发」步骤页；⑥删除为 confirm 危险确认，状态启停走 before-change 写接口；⑦**2026-09-09 勘察（云云bridge优化落地面）**：上行/下行规则抽屉标题均为「查看/编辑/新建」前缀+规则名；上行数据参数表已表格化管理（只读表格+行内编辑/删除+表上方添加参数）；转换类型动态表单三分支（枚举值映射规则/设备时间格式/无额外项）已实现，但「设备时间格式」required:false 实际非必填（优化预期必填待确认）；响应处理 tab 有「响应表达式配置」标题与 payload参数表二级标题；下行 Get Token/添加节点隐藏「指令数据」与「自定义 Body 模板」，但 **Body/Query 参数表仅由 HTTP 方法驱动、不随类型隐藏（与优化需求偏差，报告留档）**；「对应平台参数」已更名且全类型非必填、仅 Query 显示；参数说明为 span 固定展示；输出面板标题「输出下行指令」；值来源选项文案「平台生成/静态值」（需求原文「平台随机生成（随机字符串、时间戳）」，表格回显兼容标签保留全称）；⑧**云云bridgeBug 18 项源码层现状**：第 1 项重复表达式前端未见查重（预计后端拦截，运行时验证）、第 7 项 Basic Auth 密码 show-password 已实现（S2 实际渲染 DownAuthTypeBasicAuth）、第 9 项双提示已收敛为单条「添加成功」（静默保存）、第 11/13/14/15/16/17 项均已落地、第 18 项转换类型四选项齐但第 4 项文案「键值对」缺「转换」二字且顺序与需求描述不同、第 6/8 项为像素级视觉项（源码静态无重叠结构，需 MCP 截图复验）、第 2/3/4/5 项见专项用例与范围外声明。
>
> 范围外声明：**三步配置页的完整配置面**（Step1 协议信息/产品-协议映射/公共Header、Step2 上行认证+上行解析规则+数据转换预览、Step3 下行认证+下行规则+规则测试——组件面约 1.3 万行；2026-09-09 起抽屉深度面由 OP-CBP-024~039 覆盖，仍范围外：真实设备数据上报端到端转换、签名认证（Sign/SignNToken）完整配置面、动态 Token 获取链路——依赖第三方真实回调，无法合成闭环）；**协议名称唯一性/重复创建**（源码未见唯一性校验入口，刻意制造重复合成协议会污染列表——见 scope.json C03 范围外理由）；**「查看」入口**（源码已注释，功能不存在）；**云云bridgeBug 第 5 项**（删除产品同步 bridge 表：跨模块高风险删除写入，Bug 文档已标通过，见 scope.json C05 范围外理由）；**Bug 第 6/8 项像素级视觉核查**（英文间距、输入框与标题横线重合：不适合 Playwright 行为断言，由 MCP 只读勘察截图复验并出具结论，见 scope.json C04 范围外理由）。
>
> 本表供测试范围审核。真实密码、Token、密钥、Cookie 等可复用凭据仅从本地 `.env` 或对应登录态存储读取；非法值均为合成构造（D01~D05）；写入链路数据全部为合成值并经台账记录，删除仅限台账创建的合成协议。

## 快速索引

| 模块 | 用例编号 | 用例标题 | 优先级 | 风险 |
| --- | --- | --- | --- | --- |
| 协议列表 | OP-CBP-001 | 协议管理页渲染 | P0 | 低 |
| 协议列表 | OP-CBP-002 | 搜索筛选交互 | P2 | 低 |
| 新增协议 | OP-CBP-003 | 弹窗结构与字段校验（边界） | P1 | 中 |
| 新增协议 | OP-CBP-004 | 云云服务未开通提交拦截（错误路径） | P1 | 中 |
| 云云服务开通 | OP-CBP-005 | 开通云云接入服务（写入） | P0 | 中 |
| 新增协议 | OP-CBP-006 | 创建协议写入链路（写入） | P0 | 中 |
| 三步配置页 | OP-CBP-007 | 三步配置页可达与结构冒烟 | P1 | 低 |
| 协议列表 | OP-CBP-008 | 编辑回显与状态启停（写入） | P1 | 中 |
| 协议列表 | OP-CBP-009 | 删除合成协议自清理（写入） | P1 | 中 |
| 三步配置页-Step1 | OP-CBP-010 | 产品-协议映射添加与删除（写入） | P1 | 中 |
| 三步配置页-Step1 | OP-CBP-011 | 公共Header新增与删除（写入） | P1 | 中 |
| 三步配置页-Step2 | OP-CBP-012 | 上行认证配置双方式查看与保存（写入） | P1 | 中 |
| 三步配置页-Step2 | OP-CBP-013 | 上行解析规则列表初始化与启停（写入） | P1 | 中 |
| 三步配置页-Step2 | OP-CBP-014 | 上行规则编辑抽屉结构冒烟（取消不落库） | P2 | 低 |
| 三步配置页-Step3 | OP-CBP-015 | 下行通讯认证配置保存（写入） | P1 | 中 |
| 三步配置页-Step3 | OP-CBP-016 | 下行规则添加与删除（写入） | P1 | 中 |
| 三步配置页-Step3 | OP-CBP-017 | 数据转换预览与测试转换+最终保存（写入） | P1 | 中 |
| 协议详情 | OP-CBP-018 | 详情页只读渲染与翻步 | P2 | 低 |
| 协议列表 | OP-CBP-019 | 协议状态启用/禁用状态流转（写入） | P0 | 中 |
| 新增协议 | OP-CBP-020 | 新增协议弹窗字段正反与极限值（no_write 深度校验） | P1 | 低 |
| 三步配置页-Step3 | OP-CBP-021 | 下行规则表单字段正反与边界（no_write 校验） | P1 | 低 |
| 三步配置页-Step2 | OP-CBP-022 | 上行规则表单字段正反与边界（no_write 校验） | P1 | 低 |
| 三步配置页-Step1 | OP-CBP-023 | 公共Header表单字段正反与边界（no_write 校验） | P1 | 低 |
| 上行规则编辑抽屉 | OP-CBP-024 | 抽屉页面标题与三 tab 结构（no_write） | P1 | 低 |
| 上行规则编辑抽屉 | OP-CBP-025 | 上行数据参数表只读表格化与添加入口（no_write） | P1 | 低 |
| 上行规则编辑抽屉 | OP-CBP-026 | 添加上行参数弹窗必填与转换类型动态表单（no_write） | P1 | 低 |
| 上行规则编辑抽屉 | OP-CBP-027 | 上行参数添加写入与单一「添加成功」提示（写入） | P1 | 中 |
| 上行规则编辑抽屉 | OP-CBP-028 | 上行参数编辑回显与转换类型修改（写入） | P1 | 中 |
| 上行规则编辑抽屉 | OP-CBP-029 | 上行参数删除自清理（写入） | P1 | 中 |
| 上行规则编辑抽屉 | OP-CBP-030 | 同一规则下重复提取表达式拦截（Bug1，写入风险） | P1 | 中 |
| 上行规则编辑抽屉 | OP-CBP-031 | 响应处理区块结构与 payload参数表二级标题（写入：映射保存） | P2 | 中 |
| 上行规则编辑抽屉 | OP-CBP-032 | payload 参数添加写入：值来源联动与静态值（写入） | P1 | 中 |
| 上行规则编辑抽屉 | OP-CBP-033 | Basic Auth 密码显隐切换与认证区文案核查（no_write） | P2 | 低 |
| 上行规则编辑抽屉 | OP-CBP-034 | 可视化映射必填校验拦截（no_write 自适应） | P1 | 低 |
| 下行规则编辑抽屉 | OP-CBP-035 | 下行抽屉标题与「下行触发条件」小标题（no_write） | P1 | 低 |
| 下行规则编辑抽屉 | OP-CBP-036 | Get Token 规则创建与类型驱动区块显隐（写入） | P1 | 中 |
| 下行规则编辑抽屉 | OP-CBP-037 | 下行 Body 参数表列与弹窗字段核查（写入） | P1 | 中 |
| 下行规则编辑抽屉 | OP-CBP-038 | 转换类型四选项与「输出下行指令」文案核查（no_write） | P2 | 低 |
| 下行规则编辑抽屉 | OP-CBP-039 | Get Token 草稿规则删除自清理（写入） | P1 | 中 |
| Bug/优化清单专项 | OP-CBP-040 | 选择平台产品弹窗居中显示（Bug4，no_write） | P2 | 低 |
| Bug/优化清单专项 | OP-CBP-041 | 新增协议双入口弹窗一致性（Bug2，no_write） | P2 | 低 |

## 模块：协议列表

<details>
<summary>OP-CBP-001｜协议管理页渲染｜P0｜低风险</summary>

> 前置条件：已登录。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 进入协议管理页（/integration/cloud-bridge-protocol） | 无 | 标题「协议管理」与描述（配置云云接入协议转换Bridge）可见 |
| — | 2 | 观察工具栏 | 无 | 「新增协议」按钮可见 |
| — | 3 | 观察列表 | 无 | 表头齐全（序号/协议名称/协议描述/是否关联产品/状态/创建时间/操作）；空态「暂无数据」（2026-09-03 实证 0 行） |

</details>

<details>
<summary>OP-CBP-002｜搜索筛选交互｜P2｜低风险</summary>

> 前置条件：已登录，位于协议管理页。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察搜索区字段 | 无 | 搜索区渲染；字段清单注解（2026-09-03 实证：本页 search-form 为 slim 模式且无筛选字段，仅承载新增按钮插槽） |
| — | 2 | 存在筛选字段时输入过滤（否则注解跳过） | 合成值「自动化测试协议」 | 有字段则输入生效、无报错；无字段注解实证并跳过 |

</details>

<details>
<summary>OP-CBP-008｜编辑回显与状态启停（写入）｜P1｜中风险</summary>

> 前置条件：已登录，位于协议管理页；OP-CBP-006 已创建台账合成协议且未删除（候选 = 台账最新协议且当前列表可见）。
>
> 是否写入数据：是（编辑保存 + 状态启停 before-change 写接口；仅作用于本工程台账创建的合成协议）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D04 | 1 | 点击台账协议行操作列「编辑」（跳转 :id/edit 三步页，2026-09-07 实证：列表行「编辑」为路由链接而非弹窗） | 台账最新协议 | 三步页打开且回显：协议规则名称=D04 值、协议描述=D05 值（Step1 表单） |
| D06 | 2 | 修改协议描述后点「保存信息配置」 | D06 描述=合成值「自动化测试协议描述修订<runId尾6位>」 | 保存成功（无错误提示）；刷新后回显 D06 值 |
| — | 3 | 返回协议管理页，点击状态开关（启用↔禁用） | 无 | 无确认弹窗：before-change 直接调写接口，成功后开关翻转，列表状态列更新 |
| — | 4 | 再次切换恢复原状态 | 无 | 状态恢复，台账记录两次切换时间戳 |

</details>

<details>
<summary>OP-CBP-009｜删除合成协议自清理（写入）｜P1｜中风险</summary>

> 前置条件：已登录，位于协议管理页；OP-CBP-006 已创建台账合成协议（本用例删除该协议完成自清理）。
>
> 是否写入数据：是（仅删除本工程台账创建的合成协议；删除前完成 006~008 的验证）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D04 | 1 | 点击台账协议行操作列「删除」 | 台账最新协议（名称核对） | 出现 confirm 确认弹窗（危险确认文案可见） |
| — | 2 | 确认删除 | 无 | 成功提示，该行从列表消失 |
| — | 3 | 台账记录 | — | 本包 runtime/generated-data.json 追加协议记录的 deletedAt 标记（台账只增不删） |

</details>

## 模块：新增协议

<details>
<summary>OP-CBP-003｜弹窗结构与字段校验（边界）｜P1｜中风险</summary>

> 前置条件：已登录，位于协议管理页。2026-09-03 实证：协议规则名称 required（提示「请输入协议名称」）、maxlength=32；协议描述 maxlength=256。
>
> 是否写入数据：否（逐字段喂非法值断言字段级提示，不点击提交；客户端拦截例外点击确定断言拦截）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 打开「新增协议」弹窗 | 无 | 弹窗标题「新增协议」；协议规则名称 + 协议描述（textarea 5 行）字段可见 |
| D01 | 2 | 空表单点击确定 | 无 | 提交被拦截：字段级提示（请输入协议名称），弹窗不关闭 |
| D02 | 3 | 协议规则名称输入 33 个字符 | 合成值「协」×33 | maxlength=32 截断为 32 字（实证注解） |
| D03 | 4 | 恢复合法值 | 名称=合成值；描述=合成值 | 字段错误清空（不实际提交——提交链路见 OP-CBP-004/006） |
| — | 5 | 取消关闭 | 无 | 弹窗关闭，列表无新增 |

</details>

<details>
<summary>OP-CBP-004｜云云服务未开通提交拦截（错误路径）｜P1｜中风险</summary>

> 前置条件：已登录，位于新增协议弹窗；已监听网络请求（排除只读查询）。2026-09-03 只读探针实证：queryCloudAccessService 返回 openStatus=0、applicationId=null（账号未开通云云接入服务）。**若服务已开通（含本包 OP-CBP-005 开通后重跑），本用例预期自然失效：改为注解记录当前 openStatus 并跳过拦截断言（前置状态不满足时的自适应分支）。**
>
> 是否写入数据：否（服务前置检查失败，无创建请求发出；仅只读查询）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 填合法名称与描述后点击确定 | 名称=合成值「自动化测试协议校验」；描述=合成值 | 未开通：出现错误提示（无云云接入服务信息——语义一致即通过），弹窗不关闭；已开通：注解记录状态并跳过本断言 |
| — | 2 | 网络监听核对 | 无 | 未开通分支：无协议创建请求（create/authorization）发出；仅只读查询请求 |

</details>

<details>
<summary>OP-CBP-006｜创建协议写入链路（写入）｜P0｜中风险</summary>

> 前置条件：已登录，位于协议管理页；云云接入服务已开通（OP-CBP-005 完成，openStatus=1、applicationId 非空）。
>
> 是否写入数据：是（创建合成协议并经台账记录；创建成功触发授权跳转三步配置页）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D04 | 1 | 打开「新增协议」弹窗，填名称与描述后确定 | D04 名称=合成值「自动化测试协议CBP<runId尾6位>」（≤32）；D05 描述=合成值「自动化测试合成协议-云云桥接<runId尾6位>」 | 无错误提示；列表出现该协议行（未关联=info、状态=启用），台账记录协议数据 |
| — | 2 | 观察创建后跳转 | 无 | 自动跳转三步配置页（:id/edit 路由，见 OP-CBP-007） |
| — | 3 | 台账记录 | — | 本包 runtime/generated-data.json 记录协议名称/描述/配置页路由（含协议 id） |

</details>

## 模块：云云服务开通

<details>
<summary>OP-CBP-005｜开通云云接入服务（写入）｜P0｜中风险</summary>

> 前置条件：已登录。前置写入：通过创建向导创建「云云接入+自定义协议」合成产品（照明-灯/WiFi/普通设备，入台账）——开通卡片仅在该类产品的设备开发步骤页渲染。幂等分支：若服务已开通（按钮为「查看服务」），直接验证服务状态并注解跳过提交。
>
> 是否写入数据：是（开通云云接入服务：产生服务绑定 openStatus=1 + applicationId，account 级一次性动作；表单数据为合成/预填值）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D08 | 1 | 创建向导：选品类（照明-灯）→ 方案「云云接入」→ 填名称/型号 → 协议类型选「自定义协议」（云云方案独有必填项）→ 通讯协议 WiFi → 设备类型 普通设备 → 创建 | D08 产品名=合成值「自动化测试云云产品<runId尾6位>」；型号=at<runId尾6位> | toast「产品创建成功」；跳转基础配置页；该产品记入台账 |
| D08 | 2 | 进入该产品设备开发步骤页（:id/develop），观察开通入口状态 | 无 | 未开通：「开通服务」按钮可见；已开通：「查看服务」+ 服务状态「已开通」（幂等分支：注解记录后用例通过并跳过 3~5） |
| — | 3 | 打开「开通服务」弹窗，选择数据中心第一项 | 无 | 弹窗标题「开通云云服务」；项目名称固定「云云接入服务」（disabled）；数据中心下拉选择第一项；联系人/联系方式预填当前账号信息 |
| D07 | 4 | 确认开通 | D07 联系方式=账号预填合成手机号（不改预填值） | toast「云云接入服务开通成功」；步骤卡状态刷新 |
| — | 5 | 验证开通结果 | 无 | 按钮变「查看服务」；服务状态「已开通」；台账记录开通时间与数据中心 |
| — | 6 | 网络监听核对 | 无 | 开通请求为 `POST /open/internal/open/cloud/device/access/service` 且仅发出一次 |

</details>

## 模块：三步配置页

<details>
<summary>OP-CBP-007｜三步配置页可达与结构冒烟｜P1｜低风险</summary>

> 前置条件：已登录；OP-CBP-006 创建协议成功并跳转三步配置页（:id/edit）。
>
> 是否写入数据：否（仅观察页面结构，不提交任何配置——完整配置面由 OP-CBP-010~018 覆盖）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察步骤条 | 无 | StepProgress 三步可见：协议信息/上行解析/下行解析 |
| — | 2 | 观察 Step1（协议信息）区块 | 无 | 协议信息表单区块渲染（以源码 S1 组件结构为准，具体字段注解记录） |
| — | 3 | 返回协议管理页 | 无 | 台账协议行存在于列表，本包冒烟结束 |

</details>


## 模块：三步配置页-Step1（协议信息）

<details>
<summary>OP-CBP-010｜产品-协议映射添加与删除（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（取 OP-CBP-006 产出，进入其 :id/edit Step1）。
>
> 是否写入数据：是（协议的产品-协议映射 1 条增/1 条删；作用于台账合成协议；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | Step1 点击「添加映射关系」 | 无 | 映射弹窗打开，含产品选择控件 |
| D08 | 2 | 选择云云前置产品并确认 | 台账云云产品（自动化测试云云产品<runId尾6位>） | 映射列表出现该产品行（产品名称/model 列非空） |
| — | 3 | 删除该映射行 | 无 | 行消失（确认交互按实现注解记录） |

</details>

<details>
<summary>OP-CBP-011｜公共Header新增与删除（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（进入其 :id/edit Step1）。
>
> 是否写入数据：是（协议公共 Header 1 条增/1 条删；合成值；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D09 | 1 | 点击「新增 Header」填写参数并确定 | D09 Header 参数=合成值 bridge-hdr-<runId尾6位十进制转小写字母（2026-09-07 实证：参数名 pattern 仅允许 A-Z a-z _ -，不得含数字）>；值来源=静态值 | Header 列表出现该行 |
| — | 2 | 删除该 Header 行 | 无 | 行消失（确认交互按实现注解记录） |

</details>

## 模块：三步配置页-Step2（上行解析）

<details>
<summary>OP-CBP-012｜上行认证配置双方式查看与保存（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（进入其 :id/edit，query.step=2 或点下一步到 Step2）。
>
> 是否写入数据：是（协议上行认证配置保存 1 次；内容为切换后的认证方式；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察认证方式默认态 | 无 | 认证方式 radio 可见（平台标准认证方式/自定义认证方式） |
| — | 2 | 切到「平台标准认证方式」 | 无 | 平台标准认证信息区块出现（认证类型/项目ID/平台回调地址/App key/数据接收方式/App secret） |
| — | 3 | 切到「自定义认证方式」→ 上行认证方式选「Basic Auth」 | 无 | Basic Auth 配置区块出现（App key/App secret 类字段） |
| — | 4 | 点「保存认证配置」 | 无 | 保存成功提示出现（提示类断言按行为断言），无错误提示 |

</details>

<details>
<summary>OP-CBP-013｜上行解析规则列表初始化与启停（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step2）。
>
> 是否写入数据：是（上行规则状态启停切换 2 次，最终恢复原状态；作用于协议自动初始化的标准规则；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 进入 Step2 观察「上行解析规则列表」 | 无 | 列表渲染 ≥1 条标准规则（规则名称/触发条件/回调地址列非空；initBridgeRuleList 自动初始化） |
| — | 2 | 切换首行状态开关 | 无 | 开关翻转（aria-checked 变化，无确认弹窗） |
| — | 3 | 再次切换恢复原状态 | 无 | 状态恢复，台账记录两次切换时间戳 |

</details>

<details>
<summary>OP-CBP-014｜上行规则编辑抽屉结构冒烟（取消不落库）｜P2｜低风险</summary>

> 前置条件：已登录；OP-CBP-013 已见规则列表。
>
> 是否写入数据：否（抽屉只查看并取消关闭）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点首行「编辑」打开规则抽屉 | 无 | 抽屉打开，含 3 个 tab：1. 规则基础信息 / 2. 解析映射规则 / 3. 响应处理 |
| — | 2 | 逐个切换 tab 观察结构 | 无 | 各 tab 面板渲染（字段以源码为准注解记录）；第 3 tab 底部出现「完成配置」 |
| — | 3 | 取消/关闭抽屉 | 无 | 抽屉关闭；规则列表数据不变（行数与状态不变） |

</details>

## 模块：三步配置页-Step3（下行解析）

<details>
<summary>OP-CBP-015｜下行通讯认证配置保存（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）。
>
> 是否写入数据：是（协议下行认证配置保存 1 次；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 进入 Step3 观察「下行通讯认证配置」 | 无 | 认证配置表单渲染（DownAuthConfig 自定义认证字段） |
| D10 | 2 | 填写必填认证字段 | D10 合成值（字段名以实现为准，注解记录） | 字段填写成功 |
| — | 3 | 点「保存认证配置」 | 无 | 保存成功提示出现，无错误提示 |

</details>

<details>
<summary>OP-CBP-016｜下行规则添加与删除（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）。
>
> 是否写入数据：是（下行规则 1 条增/1 条删；合成值；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D11 | 1 | 点「添加下行规则」，tab1 填基础信息 | D11 规则名称=自动化测试下行规则<runId尾6位>；Control Type=首个可选项 | tab1 表单填写完成 |
| — | 2 | 逐 tab 下一步到末 tab 完成/保存 | 无 | 规则创建成功，下行规则列表出现该行 |
| — | 3 | 删除该规则行 | 无 | 确认后行消失（删除交互按实现注解记录） |

</details>

<details>
<summary>OP-CBP-017｜数据转换预览测试转换+最终保存（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）；OP-CBP-013 已初始化上行标准规则。
>
> 是否写入数据：是（handleFinalSave 最终保存协议配置 1 次；testTransform 为测试转换接口不产生业务数据；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | Step2 数据转换预览：选择测试模板样例（必选，实证：未选样例转换被拦截） | 无 | 样例选中，输入样例文本框就绪 |
| — | 2 | 点「测试转换」 | 无 | 「转换成功」与「输出三方数据」区渲染 |
| — | 3 | 翻到第 3 步点「保存」 | 无 | 保存成功提示出现；协议配置持久化（字段无残留错误） |

</details>

## 模块：协议详情

<details>
<summary>OP-CBP-018｜详情页只读渲染与翻步｜P2｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 打开 :id/detail | 无 | 三步结构渲染为只读态：S1 隐藏「保存信息配置/添加映射关系」及映射操作列（2026-09-07 实证修订：「新增 Header」按钮 View 态仍渲染——疑似实现缺口，按实证断言并注解） |
| — | 2 | 逐步下一步到第 3 步 | 无 | StepProgress 步进正常，S2/S3 只读渲染 |
| — | 3 | 返回列表 | 无 | 协议行仍存在，无数据变化 |

</details>


## 模块：协议状态流转

<details>
<summary>OP-CBP-019｜协议状态启用/禁用状态流转（写入）｜P0｜中风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（OP-CBP-006 产出，OP-CBP-009 删除前执行）。
>
> 是否写入数据：是（协议状态启停切换 2 次并回读持久化，最终恢复原状态；记台账）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 记录初始状态并切换开关（启用↔禁用） | 无 | 开关 aria-checked 翻转，状态列文案同步变化（启用/禁用），无确认弹窗 |
| — | 2 | 刷新页面回读状态 | 无 | 切换后状态持久化（与刷新前一致，非仅前端态） |
| — | 3 | 再次切换恢复原状态并刷新复核 | 无 | 状态恢复初始值，台账记录切换与恢复时间戳 |

</details>

## 模块：字段正反与边界（深度校验）

<details>
<summary>OP-CBP-020｜新增协议弹窗字段正反与极限值（no_write 深度校验）｜P1｜低风险</summary>

> 前置条件：已登录，位于新增协议弹窗。客户端校验拦截场景：不产生任何网络写入。
>
> 是否写入数据：否（非法组合不点确定或提交被客户端拦截且无写请求）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D13 | 1 | 名称空 → 点确定 | 空 | 出现「请输入协议名称」字段级提示，无创建请求 |
| D13 | 2 | 名称 1 字符（下边界内） | 1 个合成字符 | 无字段级报错 |
| D13 | 3 | 名称 33 字符（超上界）观察截断 | 33 个合成字符 | 截断为 32 字符 |
| D13 | 4 | 描述 257 字符观察截断 | 257 个合成字符 | 截断为 256 字符 |
| D13 | 5 | 名称含 emoji/空格/符号混合 | 合成混合串 | 客户端接受或字段提示（行为注解记录） |
| — | 6 | 取消关闭弹窗 | 无 | 弹窗关闭，列表无新增 |

</details>

<details>
<summary>OP-CBP-021｜下行规则表单字段正反与边界（no_write 校验）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step3）；已打开「添加下行规则」抽屉 tab1。
>
> 是否写入数据：否（校验场景保存被客户端拦截且无写请求）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D14 | 1 | 规则名称空/超界 → 触发校验 | 空值；33 字符（规则 required 且 length(1,32)） | 出现字段级提示；33 字符被 maxlength 截断为 32 |
| D14 | 2 | 规则描述 257 字符观察截断 | 257 个合成字符 | 截断为 256 字符（length(1,256)） |
| D14 | 3 | Control Type 未选 → 保存 | 无 | required 拦截，出现字段级提示，无创建请求 |
| — | 4 | 取消关闭抽屉 | 无 | 抽屉关闭，下行规则列表无新增 |

</details>

<details>
<summary>OP-CBP-022｜上行规则表单字段正反与边界（no_write 校验）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step2）；已打开上行规则编辑抽屉 tab1。2026-09-07 实证修订：标准规则「规则名称」disabled 只读。
>
> 是否写入数据：否（校验场景取消关闭，不触发 updateCBPThirdRule）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D15 | 1 | 打开标准上行规则编辑抽屉观察「规则名称」 | 无 | 规则名称只读（disabled；2026-09-07 实证：标准规则名称不可编辑，负向校验转为回调地址） |
| D15 | 2 | 回调地址非法值 → 触发下一步/完成配置校验 | 非法路径合成串（不带前导 /） | 字段级提示（apiPath 自定义校验：须以 / 开头的合法 API 路径） |
| D15 | 3 | 消息类型标识符/值 65 字符观察截断 | 65 个合成字符 | 截断为 64 字符（maxlength=64） |
| — | 4 | 取消关闭抽屉 | 无 | 抽屉关闭，规则列表数据不变 |

</details>

<details>
<summary>OP-CBP-023｜公共Header表单字段正反与边界（no_write 校验）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（Step1）；已打开「新增 Header」弹窗。
>
> 是否写入数据：否（校验场景取消关闭，不触发 createProtocolHeader）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D16 | 1 | Header 参数空 → 确定 | 空 | 字段级提示（headerKey required，maxlength=32） |
| D16 | 2 | Header 参数 33 字符观察截断 | 33 个合成字符 | 截断为 32 字符 |
| D16 | 3 | 值来源=静态值 → 静态值留空 → 确定 | 值来源=静态值，值空 | 静态值必填提示或确定被拦截（以实现为准注解） |
| D16 | 4 | 值来源=平台生成 → 观察联动 | 无 | 出现类型/单位/格式联动字段（timestamp/random 等） |
| — | 5 | 取消关闭弹窗 | 无 | 弹窗关闭，Header 列表无新增 |

</details>

## 模块：上行规则编辑抽屉（Step2 深度）

<details>
<summary>OP-CBP-024｜抽屉页面标题与三 tab 结构（no_write）｜P1｜低风险</summary>

> 前置条件：已登录；台账存在未删除合成协议（进入其 :id/edit Step2，上行解析规则列表已有标准规则）。2026-09-09 源码勘察：抽屉标题=「查看/编辑/新建」前缀+规则名（BtnEditUpRule.vue:69-78）；tab 头 pointer-events:none 仅底部按钮可切换。
>
> 是否写入数据：否（仅打开观察与取消关闭，不步进、不改值）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | Step2 上行解析规则列表点首行「编辑」 | 无 | 抽屉打开，标题为「编辑」+该规则名（如「编辑绑定回调」，优化「编辑规则页面标题」落地） |
| — | 2 | 观察 tab 结构 | 无 | 三个 tab：1. 规则基础信息 / 2. 解析映射规则 / 3. 响应处理 |
| — | 3 | 点击 tab 头 | 无 | tab 不切换（源码 tab 头不可点击，注解记录实际行为） |
| — | 4 | 取消关闭抽屉 | 无 | 出现「关闭后数据将不予保存，确定关闭此窗口？」确认；关闭后规则列表数据不变 |

</details>

<details>
<summary>OP-CBP-025｜上行数据参数表只读表格化与添加入口（no_write）｜P1｜低风险</summary>

> 前置条件：已登录；台账合成协议 :id/edit 已打开规则抽屉（沿用 OP-CBP-024 打开的抽屉，步进口径同 OP-CBP-014：步进的静默保存为数据不变的持久化）。
>
> 是否写入数据：否（不修改任何字段值；步进静默保存数据不变，沿用 OP-CBP-014 已审核口径）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 抽屉点「下一步」切到 tab2 解析映射规则 | 无 | 解析映射区块渲染：原始数据示例/可视化映射可见 |
| — | 2 | 观察「上行数据参数表」 | 无 | 标题可见；列=提取表达式(JSONPath)/数据类型/键值对模式/转换类型/操作；单元格为只读文本非输入框（优化「表格只读不可直接编辑」落地） |
| — | 3 | 观察表上方「添加参数」入口 | 无 | 入口可见（源码为 div+图标非 button 元素，注解记录） |
| — | 4 | 观察行内操作列 | 无 | 每行提供「编辑」「删除」（优化「仅能通过按钮操作」落地） |
| — | 5 | 取消关闭抽屉 | 无 | 关闭确认后规则列表数据不变 |

</details>

<details>
<summary>OP-CBP-026｜添加上行参数弹窗必填与转换类型动态表单（no_write）｜P1｜低风险</summary>

> 前置条件：已登录；抽屉已切到 tab2（沿用 OP-CBP-025）。2026-09-09 源码勘察：弹窗字段 提取表达式（maxlength=64）/数据类型/键值对模式/转换类型（默认「无转换」）四项，无「是否必填」字段（实证）；枚举值映射行两端均为输入框；设备时间格式 required:false。探索实证：转换类型下拉点击后需等待渲染（约 0.9s）再取选项。
>
> 是否写入数据：否（校验场景由弹窗内校验拦截，不产生保存；动态表单仅切换观察）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D17 | 1 | tab2 点「添加参数」打开弹窗，不填写直接点确定 | 空 | 弹窗不关闭；出现字段级提示：请输入提取表达式/请选择数据类型/请选择键值对模式（转换类型默认无转换不提示）；无网络写入 |
| D17 | 2 | 提取表达式输入 65 字符观察截断 | 65 个合成字符 | 截断为 64 字符（maxlength=64 + 字数计数器 0/64） |
| D17 | 3 | 观察转换类型=无转换 | 无 | 无额外配置项（需求「选择无转换无需展示额外配置项」落地） |
| D18 | 4 | 转换类型切「枚举值转换」 | 无 | 出现「枚举值映射规则」区：行文案「当设备上报数值为[输入]时，映射为本平台数值[输入]」+ 添加映射规则/删除 |
| D18 | 5 | 映射行留空点确定 | 空 | 被拦截：出现「请完善所有枚举值映射规则」类提示（文案语义匹配），弹窗不关闭 |
| D19 | 6 | 转换类型切「时间格式转换」 | 无 | 出现「设备时间格式」输入（placeholder 例如：yyyy-MM-dd HH:mm:ss）；必填性注解（源码 required:false，与优化预期「需要补充」是否必填待确认，差异注解） |
| D20 | 7 | 转换类型切「键值对」再切回「无转换」 | 无 | 键值对无额外配置项（注解）；切回无转换后额外配置消失（切换重置落地） |
| — | 8 | 取消关闭弹窗 | 无 | 弹窗关闭，参数表无新增 |

</details>

<details>
<summary>OP-CBP-027｜上行参数添加写入与单一「添加成功」提示（写入）｜P1｜中风险</summary>

> 前置条件：已登录；抽屉 tab2 已打开（台账合成协议）。写入接口 `POST /bridge/cloud/param/batch/add-or-update`（静默保存）。
>
> 是否写入数据：是（台账协议新增上行参数 1 条；记台账 configWrites.param-add）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D17 | 1 | 点「添加参数」，填写后确定 | 提取表达式=`$.at<runId尾6位>`；数据类型=字符串；键值对模式=仅值(value)；转换类型=无转换（D17） | 弹窗关闭；参数表出现该行 |
| D17 | 2 | 捕获成功提示（单次竞速） | 无 | 仅出现一条「添加成功」提示，无「保存成功」并存（Bug 第 9 项复验；文案差异注解） |
| — | 3 | 台账记录 | — | 本包 runtime/generated-data.json 追加 param-add |

</details>

<details>
<summary>OP-CBP-028｜上行参数编辑回显与转换类型修改（写入）｜P1｜中风险</summary>

> 前置条件：已登录；OP-CBP-027 已添加参数行。
>
> 是否写入数据：是（修改台账协议既有上行参数 1 次；记台账 configWrites.param-edit）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D17 | 1 | 点该行「编辑」 | OP-CBP-027 参数行 | 弹窗标题「编辑上行数据参数」；表单回显 D17 现有配置（优化「编辑弹窗预填充」落地） |
| D18 | 2 | 转换类型改「枚举值转换」，完善一条映射后确定 | 设备值=合成值 0；平台值=合成值 1（D18） | 弹窗关闭；仅一条「编辑成功」提示；表格行转换类型列更新为「枚举值转换」 |
| — | 3 | 台账记录 | — | 记录 param-edit |

</details>

<details>
<summary>OP-CBP-029｜上行参数删除自清理（写入）｜P1｜中风险</summary>

> 前置条件：已登录；OP-CBP-028 已修改参数行（本轮添加的合成参数）；**排在 OP-CBP-032 之后执行**（031 拦截实证与 032 等价核查用毕该参数行语义后，由本用例删除自清理）。删除接口 `DELETE /bridge/cloud/param/delete`。
>
> 是否写入数据：是（删除本轮创建的合成参数 1 条，自清理；记台账 configWrites.param-delete）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点该行「删除」 | 无 | 出现确认弹窗「确定要删除该参数吗？删除后将无法恢复。」 |
| — | 2 | 确认删除 | 无 | 「删除成功」提示；该行从参数表消失 |
| — | 3 | 台账记录 | — | 记录 param-delete（只增不删） |

</details>

<details>
<summary>OP-CBP-030｜同一规则下重复提取表达式拦截（Bug1，写入风险）｜P1｜中风险</summary>

> 前置条件：已登录；OP-CBP-027 的表达式仍存在（在 029 删除前执行或使用独立表达式）。Bug 文档第 1 项标注（通过）：同一规则下参数表达式不能重复配置；2026-09-09 源码勘察前端未见查重，预计后端拦截，运行时验证。
>
> 是否写入数据：写入风险（预期被拦截无写入；若实现缺失导致重复行落库，判失败并立即删除重复行自清理，记疑似缺陷）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点「添加参数」，提取表达式与既有参数相同，其余合法，确定 | 提取表达式=复用 OP-CBP-027 表达式；其余=合法合成值 | 被拦截：前端字段提示或后端拒绝提示（语义匹配「重复/已存在」任一），参数表行数不变 |
| — | 2 | 失败兜底清理 | 无 | 若意外出现重复行：立即删除该行并注解「疑似缺陷：重复表达式未拦截」；台账记录失败尝试不留痕、清理写入留痕 |

</details>

<details>
<summary>OP-CBP-031｜响应处理区块结构与 payload参数表二级标题（写入：模板映射保存）｜P2｜中风险</summary>

> 前置条件：已登录；抽屉 tab2 已打开；**OP-CBP-027 已添加上行参数（步进校验要求 ≥1 条）**。2026-09-09 探索实证：tab2→tab3 步进为双层校验（参数空与模板空；源码勘察 :328 模板非空 + :336 JSON 合法）。**2026-09-10 四轮回归实证修订：标准规则「平台标准JSON Body模板」仅预填充可视化节点视图（deviceSn/sequenceId 等字段说明），不含 {{变量}} 占位、映射计数为 0——仅点「下一步」不放行，须经「插入变量」把已有参数映射进模板后方可步进（操作指南「第三步：插入变量执行映射」）**（tab2 首个 textarea 是「第三方上行数据示例」，保持留空）。2026-09-09 源码勘察：响应处理 tab 含 响应表达式配置/成功条件表达式/是否需要平台响应数据/参数数值配置/payload参数映射（payload参数表二级标题）/平台标准参数表/第三方payload模板。**2026-09-10 五轮回归实证修订：插入变量为行内气泡（参数选中+取值模式 value，无确认按钮），选中后模板节点未见 {{变量}} 落点，点「下一步」被拦截且出现短暂红条「请在平台标准JSON Body模板中配置解析规则（/映射规则）」约 3s 自动消失（trace 帧实证）——该气泡交互无法被 Playwright 确认落点，tab3 响应处理区块的自动化路径不通，按拦截实证收口；响应处理区块结构以源码勘察+截图注解留档，payload 参数弹窗语义由 OP-CBP-037（同组件 PayloadParamEditDialog）等价覆盖。**
>
> 是否写入数据：是（tab2→tab3 步进触发映射静默保存 1 次（saveFormatConfigMapping），数据不变持久化；记台账 configWrites.mapping-save）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 确认可视化映射区可见，点「插入变量」打开行内气泡 | 参数=$.at<runId尾6位> | 气泡展开并显示参数胶囊选项+取值模式 value（20260910 实证：模板仅预填可视化节点视图，无 {{变量}} 占位） |
| — | 2 | 点击气泡内 OP-CBP-027 参数胶囊，变量落点至焦点字段（deviceSn 必填位） | 参数=$.at<runId尾6位> | 模板区出现参数引用（落点=操作指南「变量自动插入到JSON模板」落地；人工截图实证 payload.thirdPartyDevIds 落入 deviceSn） |
| — | 3 | 点「保存」保存模板映射 | 无 | 保存成功；台账记录 mapping-save（保留至 009 删协议兜底清场） |
| — | 4 | 点「下一步」步进 tab3，观察默认态 | 无 | 步进放行（参数非空+映射已落点）；「响应表达式配置」区可见：成功条件表达式输入框 + 是否需要平台响应数据 radio（**默认否 → payload 区块条件隐藏**，20260910 十一轮实证：tab3 默认仅此一区，底部按钮为「完成配置」） |
| — | 5 | 点「是否需要平台响应数据=是」展开条件区块并观察 | 无 | payload参数表二级标题出现（Bug 第 11 项核查），列=序号/提取表达式(JSONPath)/数据类型/键值对模式/值来源/是否必填/转换类型/操作；平台标准参数表列=序号/参数名称/提取表达式(JSONPath)/数据类型/参数位置/是否必须；第三方payload模板区可见（两表列结构不同，分别断言） |
| — | 6 | 不点「完成配置」，直接关闭抽屉 | 无 | 抽屉关闭（关闭确认）；不把「是」分支态持久化（031 映射已在步骤 3 保存，radio 态变更不落库） |

</details>

<details>
<summary>OP-CBP-032｜payload 参数添加写入：值来源联动与静态值（写入）｜P1｜中风险</summary>

> 前置条件：已登录；抽屉 tab3 已打开（**依赖 OP-CBP-031 已完成插入变量气泡落点与映射保存**，步进校验放行；tab3 默认 radio「是否需要平台响应数据=否」payload 区隐藏，本用例步骤 0 先切「是」展开——031 已实证条件渲染）。写入接口同 OP-CBP-027（param/batch/add-or-update）。2026-09-09 源码勘察：弹窗实际标题「添加Body参数」（共用组件默认标题，与 payload 语境的文案差异注解）；上行/下行参数表共用 `PayloadParamEditDialog.vue`。
>
> 是否写入数据：是（台账协议新增 payload 参数 2 条并自清理删除；记台账 configWrites.payload-param-add/delete）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D25 | 1 | payload参数表点「添加参数」，值来源选「平台生成」 | 提取表达式=`$.resp<runId尾6位>`；数据类型=字符串；键值对模式=仅值；转换类型=无转换；值来源=平台生成→类型=时间戳、单位=秒级（D25） | 出现类型/单位子配置（联动落地）；确定后仅一条「添加成功」；行出现（值来源列显示兼容标签「平台随机生成（时间戳）」，注解） |
| D26 | 2 | 再点「添加参数」，值来源选「静态值」 | 提取表达式=`$.stc<runId尾6位>`；其余同上；静态值=合成值 ok（D26） | 出现静态值输入；确定后行出现，仅一条「添加成功」 |
| — | 3 | 删除两条参数自清理 | 无 | 每条删除有确认弹窗；确认后行消失；台账记录 add×2 + delete×2 |

</details>

<details>
<summary>OP-CBP-033｜Basic Auth 密码显隐切换与认证区文案核查（no_write）｜P2｜低风险</summary>

> 前置条件：已登录；台账合成协议 :id/edit Step2 上行通信认证配置区（排在本链 OP-CBP-012 之后执行，012 已将认证持久化为「自定义认证方式-Basic Auth」，本用例幂等复用；若当前为其他认证方式则按步骤 1 切换）。2026-09-09 探索实证：认证方式为 radio（平台标准认证方式/自定义认证方式）+「上行认证方式」select（选项=签名认证/Basic Auth）两级；Basic Auth 块含 用户名/密码/加密算法/字符串连接符/认证头 五项必填；密码框 show-password 切换实测 type=password→text 生效。
>
> 是否写入数据：否（仅切换显示状态与观察，不点「保存认证配置」；结束态与打开时一致）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 确认认证方式为「自定义认证方式」+「Basic Auth」（非该态则切换） | 无 | Basic Auth 配置区出现（用户名/密码/加密算法/字符串连接符/认证头，实证 5 项必填） |
| — | 2 | 密码框输入合成值后点击显隐切换图标 | 合成值（非凭据，仅本地回显测试） | 输入框在密文/明文间切换（input type=password↔text，Bug 第 7 项核查：探索已实证切换生效） |
| — | 3 | 观察认证区提示文案 | 无 | 不出现「AuthorizationToken 所在 header 自动加入公共 header」类提示（Bug 第 12 项核查：探索实证不存在）；现文案为「编码结果默认放置在"Authorization" header内」（注解） |
| — | 4 | Basic Auth 区标签视觉注解 | 无 | 「Basic Auth」「加密算法」「字符串连接符」「认证头」等标签截图留档注解（Bug 第 6 项，视觉项不做行为断言；签名认证块「App Key」类英文标签仅在签名认证方式下出现，若需核查则切签名认证后截图注解） |
| — | 5 | 不点「保存认证配置」 | 无 | 无认证保存请求；结束态与打开时持久化状态一致 |

</details>

<details>
<summary>OP-CBP-034｜可视化映射必填校验拦截（no_write 自适应）｜P1｜低风险</summary>

> 前置条件：已登录；抽屉 tab2 已打开（台账合成协议，**排在本链 OP-CBP-027 之前执行**——参数表为空时拦截分支必现）。2026-09-09 探索实证：步进（2→3）校验为双层（第一层：参数表非空，提示「请在上行数据参数表中至少添加一个参数」；第二层：Body 模板非空/逐行三项/JSON 合法），校验失败停留 tab2 无映射保存请求。**自适应分支**：若既有配置已完整，拦截分支前置不成立，注解记录后跳过拦截断言。
>
> 是否写入数据：否（拦截分支无写入；自适应放行分支的静默保存为数据不变持久化，注解）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察 tab2 可视化映射区 | 无 | 平台标准JSON Body模板渲染；保存/刷新图标按钮可见 |
| — | 2 | 配置不完整时（参数表无有效行或模板为空）直接点「下一步」 | 无 | 步进被拦截：出现校验提示（参数空时实证文案「请在上行数据参数表中至少添加一个参数」；模板空时为模板非空语义），停留 tab2，无映射保存请求 |
| — | 3 | 取消关闭抽屉 | 无 | 关闭确认后数据不变 |

</details>

## 模块：下行规则编辑抽屉（Step3 深度）

<details>
<summary>OP-CBP-035｜下行抽屉标题与「下行触发条件」小标题（no_write）｜P1｜低风险</summary>

> 前置条件：已登录；台账合成协议 :id/edit Step3 下行规则列表。2026-09-09 源码勘察：新建态标题「新建下行规则配置」，编辑态「编辑{规则名}」；「下行触发条件」小标题在 tab1（Bug 第 13 项）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点「添加下行规则」打开抽屉 | 无 | 抽屉打开，标题「新建下行规则配置」（新建态兜底文案，注解） |
| — | 2 | 观察 tab1 | 无 | 「下行触发条件」小标题可见（Bug 第 13 项核查）；Control Type 选项 5 项：设备绑定/设备解绑/属性控制/添加节点/Get Token；**回归实证 tab 结构：1. 规则基础信息 / 2. 下行协议构建 / 3. 响应处理（036 快照）** |
| — | 3 | 不填写直接取消抽屉 | 无 | 「关闭后数据将不予保存」确认；关闭后下行规则列表无新增 |
| — | 4 | 点已有规则行「编辑」 | 无 | 标题=「编辑」+该规则名（优化「编辑下行规则」标题以动态前缀落地，注解） |

</details>

<details>
<summary>OP-CBP-036｜Get Token 规则创建与类型驱动区块显隐（写入）｜P1｜中风险</summary>

> 前置条件：已登录；台账合成协议 :id/edit Step3。D33=系统级类型（Get Token）+POST。创建写入 `POST /bridge/cloud/third/rule/add`（tab1→tab2 步进触发）。2026-09-09 探索实证：创建后抽屉标题变为「新建{规则名}」；tab2 含 必填「业务数据反馈」radio（同步/异步）。
>
> 是否写入数据：是（新建 Get Token 草稿规则 1 条，OP-CBP-039 删除自清理；记台账 configWrites.down-draft-add）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D33 | 1 | 点「添加下行规则」，tab1 填基础信息后点「下一步」 | 规则名称=自动化测试GetToken规则<runId尾6位>；Control Type=Get Token（D33） | 进入 tab2；规则已创建（createCBPThirdRule），列表出现该草稿行；抽屉标题变为「新建{规则名}」（探索实证，注解） |
| D33 | 2 | 观察 tab2 区块 | 无 | 「指令数据」（平台参数表）不渲染、「自定义 Body 模板」不渲染（系统级类型隐藏，优化下行瘦身落地，探索实证）；必填「业务数据反馈」radio（同步/异步）可见 |
| D33 | 3 | HTTP 方法选 POST 后观察参数表 | 无 | Body 参数表渲染、Query 参数表不渲染（方法驱动，探索实证；**实现差异注解：Body 参数表不随 Get Token 类型隐藏，与优化需求「不展示 body 参数表」偏差，报告留档**） |
| — | 4 | 取消关闭抽屉（不做完成配置） | 无 | 关闭确认后草稿规则保留在列表（已落库，039 清理） |
| — | 5 | 台账记录 | — | 记录 down-draft-add（GetToken 草稿） |

</details>

<details>
<summary>OP-CBP-037｜下行 Body 参数表列与弹窗字段核查（写入）｜P1｜中风险</summary>

> 前置条件：已登录；OP-CBP-036 的 Get Token 草稿规则存在。2026-09-09 源码勘察：Body 表无「对应平台参数」列；Query 表有且仅 Query 弹窗显示该字段（全类型非必填，placeholder 请选择对应平台参数）。探索实证：Body 表头=提取表达式(JSONPath)/数据类型/是否必填/键值对模式/值来源/转换类型/操作；弹窗「是否必填」radio 默认必填；「值来源」无必填星号。
>
> 是否写入数据：是（草稿规则新增 Body 参数 1 条并自清理删除；记台账 configWrites.down-param-add/delete）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 打开草稿规则编辑，切到 tab2 观察 Body 参数表 | 无 | 列=提取表达式(JSONPath)/数据类型/是否必填/键值对模式/值来源/转换类型/操作（探索实证）；无「对应平台参数」列（优化三列落地；Bug 第 14/15 项） |
| — | 2 | 点「添加参数」，空表单确定 | 空 | 弹窗不关闭：出现 请输入提取表达式/请选择数据类型/请选择键值对模式 提示（转换类型默认无转换、是否必填默认必填均不提示，探索实证）；无「对应平台参数」字段（Body 态，探索实证） |
| — | 3 | 填写合法值后确定 | 提取表达式=`$.body<runId尾6位>`；数据类型=字符串；键值对模式=仅值；转换类型=无转换；值来源=静态值+合成值 | 弹窗关闭，Body 表出现该行；台账记录 down-param-add |
| D34 | 4 | HTTP 方法切 GET（不切换 tab、不保存） | 无 | Query 参数表替代 Body 表渲染，**Query 表列头含「对应平台参数」列（Bug 第 14/15 项：表格层保留）；Query「添加参数」弹窗内该字段仅非 GetToken/添加节点 规则显示（20260910 二轮实证：本轮草稿为 GetToken，弹窗无该字段，与源码条件渲染一致，注解留档）；值来源标签无必填星号实证**；取消弹窗 |
| — | 5 | 方法切回 POST，取消抽屉（关闭确认，丢弃未保存修改） | 无 | 方法修改不落库（注解）；规则列表数据不变 |
| — | 6 | 删除 Body 参数行自清理 | 无 | 确认弹窗后「删除成功」，行消失；台账记录 down-param-delete |

</details>

<details>
<summary>OP-CBP-038｜转换类型四选项与「输出下行指令」文案核查（no_write）｜P2｜低风险</summary>

> 前置条件：已登录；台账合成协议 :id/edit Step3；**排在本链 OP-CBP-037 之后执行**（复用其打开的草稿规则与参数弹窗，转换类型下拉展开即含四选项）。Bug 第 17/18 项核查。2026-09-09 探索实证：S3 页面在未打开规则测试区时不渲染输出面板（「输出下行指令」「输出原始数据」均不出现）——输出面板随 DownRuleTest 规则测试区渲染。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 在草稿规则 tab2「添加参数」弹窗展开「转换类型」下拉（点击后等待渲染再取选项） | 无 | 四个选项：无转换/枚举值转换/时间格式转换/键值对（Bug 第 18 项：四选项统一落地；文案差异注解：第 4 项为「键值对」非「键值对转换」且顺序与需求描述不同）；取消弹窗 |
| — | 2 | 关闭抽屉，观察 S3 规则测试区（DownRuleTest）输出面板 | 无 | 若面板可见：输出面板标题为「输出下行指令」（Bug 第 17 项核查）；面板不可见则点草稿行「测试」/「规则测试」入口打开后核查；入口不存在则注解「规则测试区入口未找到，Bug 第 17 项以源码证据（DownRuleTest.vue 输出面板标题）+ 后续人工复核留档」并按 Bug 文档标注核对 |
| — | 3 | 全程无写入复核 | 无 | 无下行规则/参数变更请求 |

</details>

<details>
<summary>OP-CBP-039｜Get Token 草稿规则删除自清理（写入）｜P1｜中风险</summary>

> 前置条件：已登录；OP-CBP-036 创建的 Get Token 草稿规则存在（037 完成后执行）。删除接口 `DELETE /bridge/cloud/third/rule/delete`。
>
> 是否写入数据：是（删除本轮创建的草稿规则，自清理；记台账 configWrites.down-draft-delete）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 下行规则列表找到草稿行（名称核对），点「删除」 | OP-CBP-036 规则名 | 出现 danger 确认弹窗「确定要删除规则吗?」 |
| — | 2 | 确认删除 | 无 | 「删除成功」；该行从列表消失 |
| — | 3 | 台账记录 | — | 记录 down-draft-delete；复核列表无本轮残留合成规则 |

</details>

## 模块：Bug/优化清单专项

<details>
<summary>OP-CBP-040｜选择平台产品弹窗居中显示（Bug4，no_write）｜P2｜低风险</summary>

> 前置条件：已登录；台账合成协议 :id/edit Step1 产品-协议映射区。Bug 文档第 4 项标注（通过）：弹窗页面居中显示。
>
> 是否写入数据：否（仅打开弹窗量测后取消）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点「添加映射关系」打开弹窗 | 无 | 弹窗打开且可选产品 |
| — | 2 | 量测弹窗 boundingBox 与视口 | 无 | 弹窗水平居中（|中心x−视口中心x|≤20px）；垂直方向量值注解（Bug 第 4 项复验：不再过份靠上） |
| — | 3 | 取消弹窗 | 无 | 弹窗关闭，映射列表无新增 |

</details>

<details>
<summary>OP-CBP-041｜新增协议双入口弹窗一致性（Bug2，no_write）｜P2｜低风险</summary>

> 前置条件：已登录；台账存在云云产品（自动化测试云云产品，台账 preconditionProductName）。Bug 文档第 2 项标注（通过）：两入口弹窗内容、样式一致；2026-09-09 源码勘察两入口复用同一 BtnEditCloudProtocol 组件。
>
> 是否写入数据：否（仅打开观察与取消）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 协议管理页点「新增协议」 | 无 | 弹窗打开：标题「新增协议」+ 协议规则名称/协议描述字段 + 确定/取消；记录结构快照后取消 |
| — | 2 | 经云云产品设备开发页（:id/develop）打开协议表单弹窗 | 无 | 弹窗字段结构与入口一一致（标题/字段/必填标识/按钮） |
| — | 3 | 比对两快照 | 无 | 内容一致；样式差异（如有）注解记录 |

</details>

## 数据说明

- D01~D03、D04~D16 均为合成构造值；D04/D06/D09/D11/D13/D14/D15/D16 含 runId 尾 6 位或固定合成值防撞设计；写入数据经 `src/support/recordGeneratedData.ts` 记录到本包 `runtime/generated-data.json`（只增不删）。
- **2026-09-09 新增数据编号**（云云bridge优化/Bug 清单轮）：
  - D17~D20＝上行参数弹窗「转换类型×键值对模式」有效组合（键值对模式与数据类型正交取代表 仅值+字符串）：D17 无转换+仅值、D18 枚举值转换+仅值（映射 0→1 合成）、D19 时间格式转换+仅值、D20 键值对+仅值；
  - D25~D32＝payload/下行参数弹窗「转换类型×值来源」有效组合：D25 无转换+平台生成（时间戳/秒级）、D26 无转换+静态值、D27 枚举值转换+平台生成、D28 枚举值转换+静态值、D29 时间格式转换+平台生成、D30 时间格式转换+静态值、D31 键值对+平台生成、D32 键值对+静态值（OP-CBP-032 执行 D25/D26，其余组合由弹窗观察覆盖并注解）；
  - D33~D36＝下行「类型等价类×方法等价类」有效组合：D33 系统级+POST（OP-CBP-036 执行）、D34 系统级+GET（OP-CBP-037 方法切换观察）、D35 设备级+GET、D36 设备级+POST（设备级类型维度由既有 OP-CBP-016 覆盖，注解留档）。
- 所有新增表达式/静态值/规则名均含 runId 尾 6 位防撞；**脚本执行口径（2026-09-09 回归实证）：链内数据尾号一律取台账协议名尾 6 位（writeRunId 为 worker 级常量，worker 重启后漂移会断链）**；全部为合成值，无真实凭据。
- OP-CBP-005 开通动作依赖账号当前状态（一次性）；重跑时走幂等分支（已开通即验证通过）。
- **新增用例执行顺序约束**（2026-09-09 探索实证后的链内排序）：024→025→026→034（参数空拦截）→027（加参数）→028→030（重复表达式，参数仍在）→031（气泡落点+保存映射）→032（payload 参数）→029（删上行参数）→033→035→036→037→038→039→040→041；全部插在 OP-CBP-018 与 OP-CBP-009（删协议）之间。031 的 mapping-save 模板合成值保留至 009 删协议兜底清场。
