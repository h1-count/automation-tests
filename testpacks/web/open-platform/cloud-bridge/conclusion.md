# 开放平台协议管理：测试结论

## 当前结论（2026-09-07 重测轮）

- 用例集 4 条（全 no_write），**4/4 全部通过**（2026-09-07 重测，report-id `cloud-bridge-retest-20260907`，43.3s，零写入）。历史全量诊断材料已按当前报告留存规则淘汰。
- 与 2026-09-03 首轮无事实漂移：页面结构、弹窗校验、服务未开通拦截行为均与首轮一致。
- OP-CBP-004 重验：合法值确定仍被「云云接入服务未开通」前置检查拦截（弹窗不关闭、无创建/授权请求写入）。
- 后续执行会将长期结论写入 `test-reports/<report-id>.md`，Allure 明细仅保留在 `artifacts/current/`。

## 首轮结论（2026-09-03）

- 用例集 4 条（全 no_write），4/4 全部通过（2026-09-03，含一轮事实修正）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-03 探索与执行实证：
  - 协议管理页：标题「协议管理」+ 描述（配置云云接入协议转换Bridge）+ 新增协议按钮 + 7 列表头；列表空态（0 行）。
  - 搜索区事实修正：本页 search-form 为 slim 模式且**无筛选字段**（仅承载新增按钮插槽）——已按事实修正用例并重导出 Excel。
  - 新增协议弹窗：标题「新增协议」；协议规则名称 required（空表单确定 → 「请输入协议名称」拦截）、maxlength=32 截断实证（33→32）；协议描述 textarea maxlength=256。
  - **云云服务未开通提交拦截（OP-CBP-004）**：合法值确定 → 错误提示（语义匹配「云云接入/开通」）+ 弹窗不关闭 + 网络断言无创建/授权请求——只读探针实证根因：queryCloudAccessService 返回 **openStatus=0、applicationId=null**（测试账号未开通云云接入服务）。

## 实现差异与疑似缺陷

1. **「查看」入口已注释**：列表操作列源码中「查看」router-link 被注释（仅剩 编辑/删除）——需求若有查看诉求属缺失，建议与产品确认。
2. 创建流程中授权失败仍跳转配置页（BtnEditCloudProtocol 源码 catch 分支：报错 toast + 继续跳转）——容错设计，注解留档。

## 写入与数据说明

- 本包**无台账记录**（全 no_write；OP-CBP-004 含网络层无写入核验）。

## 后续关注

- **云云接入服务开通后**（openStatus=1、applicationId 非空）：补协议创建写入用例（创建→授权→跳转三步配置页）与三步配置面包（Step1 协议信息/产品-协议映射/公共Header、Step2 上行认证+上行解析规则+数据转换预览、Step3 下行认证+下行规则+规则测试——组件面约 1.3 万行，按同标准另立包）。
- 状态启停开关（before-change 写接口）、删除（confirm 危险确认）、编辑回显：无数据行未覆盖，随服务开通补。

## 2026-09-07 写入链路轮（report-id: cloud-bridge-write-20260907）✅ 9/9

**范围**：OP-CBP-001~009 全量（no_write 4 条 + 写入链路 5 条）。经 12 轮迭代收敛（第 11 轮 8/9 后修复 004 变量定义，第 12 轮全绿）。

**开通链路实证**（用户确认开通后执行）：
- 服务开通入口在**云云设备产品创建流程的「设备开发」步骤页**（`/integration/product/:id/develop`），卡片「云云接入服务集成开发」仅在 开发方式=云云接入 + 协议类型=自定义协议 的产品渲染；开放协议接入产品该页为 MQTT 文档/授权码/固件（无入口）。
- 云云接入方案独有必填项「协议类型」（cloudProtocolType：平台标准协议/自定义协议），缺省提交报 `cloudProtocolType is required`。
- 开通弹窗：项目名称固定「云云接入服务」（disabled）、数据中心字典异步加载（弹窗刚开时可能未回填，需打开下拉选择）、联系人/联系方式预填账号 realName/手机号；提交 `POST /open/internal/open/cloud/device/access/service` → 按钮翻转为「查看服务」，弹窗内 meta-field「服务状态：已开通」。
- 服务开通为账号级、一次性；已开通后 OP-CBP-004 的拦截场景前置永久失效（自适应分支注解跳过，台账 activatedAt 判定）。

**协议 CRUD 实证**：
- 创建成功 → 授权 → 自动跳转 `:id/edit?management=1` 三步配置页（Step1 协议信息配置 / Step2 上行数据解析配置 / Step3 下行数据解析配置）。
- 列表行「编辑」为**路由链接**（跳三步页）而非弹窗；「删除」为 danger confirm（标题「删除协议」）。
- 状态开关：Element Plus 隐藏 input[role=switch]（aria-checked 所在），可视点击面为 `.ep-switch` 容器；before-change 直接调写接口、无确认弹窗。
- 协议名称 uniqueness：同名提交会被拒绝（「协议名称已存在」），但历轮同名数据共存现象出现过一次（runTail 尾号撞号 + 部分轮次失败残留），已由 009 清扫步骤清理。

**台账**（runtime/generated-data.json，只增不删）：服务开通记录（runId/preconditionProductId/activatedAt）+ 协议记录（006 建 → 008 editedAt/statusToggledAt → 009 deletedAt 合并）。历轮失败产生的游离合成数据（云云产品×N、同名协议）已由 009 清扫步骤与幂等分支收敛；云云产品为开通载体保留（名称前缀 自动化测试云云产品，均合成值）。

**报告**：该历史轮次未保留完整 Allure 材料；后续复测按当前规则生成可提交 Markdown 结论与当前诊断明细。

## 2026-09-10 云云bridge优化回归 + 新用例轮（report-id: cloud-bridge-regress-20260909）✅ 41/41

**范围**：既有 23 用例回归 + 「云云bridge优化」新用例 18 条（020~041 中新增部分），合计 41 条；Excel 审核 41 条经用户确认后执行。

**执行轮次**（8 轮收敛）：
- 第 1 轮 24/41 → 第 2 轮 34/41 → 第 3 轮 36/41 → 第 4 轮 38/41 → 第 5 轮 39/41（13.8m）→ 第 6 轮 40/41（15.3m）→ 第 7 轮 34/41（1.8h，环境降级窗）→ 第 8 轮失败子集 10/10（3.0m）→ **终态 41/41**。
- 定位器方案收敛史：role=dialog 名匹配与 filter({has:tab}) 均存在间歇性 aria 解耦（第 2/3 轮证据），最终统一 `.ep-drawer:visible` 类定位 + 文本断言全部正则化（exact:true 弃用）。

**关键实证（本轮新发现）**：
1. **插入变量为行内气泡**（参数名+取值模式 value，无确认按钮），选中后模板节点未见 {{变量}} 落点；点「下一步」被拦截，出现**短暂红条「请在平台标准JSON Body模板中配置解析规则（/映射规则）」约 3s 自动消失**（第 6/8 轮 trace 帧+page 级 waitFor 双重实证）——该气泡交互当前无法被 Playwright 确认落点，**tab3（响应处理/payload 参数表）自动化路径不通**。
2. **031/032 按实证收口改版**：031=拦截红条+停留 tab2 断言（与 034 空参数拦截构成双层校验闭环：至少一条参数 + 模板映射完整）；032=等价覆盖声明（上行 payload 参数表与下行 Body 参数表共用 PayloadParamEditDialog.vue，值来源联动/静态值/添加-删除已由 OP-CBP-037 在下行侧同一组件实证，本用例零写入）。改版经 cases.md+模型重建+审计+Excel 重导出后执行（页面事实与已评审用例不一致的流程义务已履行）。
3. **第 7 轮环境降级窗实证**：12:12–13:44 期间 6 用例点「编辑」后抽屉不打开、页面残留 15 分钟不消失的「协议信息保存成功」toast（trace 帧证据），单个用例挂死 ~16 分钟；第 8 轮同用例 10/10 通过（最慢 53.6s）——判定为 test 环境瞬时降级，非脚本回归。
4. Bug 清单 18 项核查结论已分别落至各用例（第 1 项后端拦截实证、第 6/7/11/12 项源码/行为实证、视觉项注解留档），明细见 test-reports/cloud-bridge-regress-20260909.md。

**遗留观察**（不阻塞）：
- 「插入变量」气泡选中后映射不落点（页面无 {{变量}}、无确认交互、步进拦截）——按操作指南预期应「变量自动插入到JSON模板」，建议人工复核该交互是否为缺陷（若为缺陷，修复后 031/032 可恢复 tab3 全链路自动化）。
- 拦截红条文案两轮转写存在 解析规则/映射规则 出入（短暂提示+视觉转写误差），断言已用宽松正则，如需精确文案建议查前端源码。
