# 用例集：开放平台产品基础配置（product-basic）

> 测试类型：Web ｜ 默认环境：test ｜ 默认数据策略：no_write
>
> 上下文来源：`sources/open-platform/需求/产品接入系统功能需求说明/产品接入系统功能需求说明.md`（「基础配置」章节）、前端源码 `web-open-platform/src/pages/integration/product/ProductPhaseBasic.vue`、`ProductIntegration.vue`、`components/ProductMetaInfo.vue`、`components/BtnProductSwitch.vue`、`components/BtnPublishProduct.vue`、`src/routes/integration.ts`、`src/support/recordGeneratedData.ts`、`experience/general.md`、`experience/open-platform.md`；查重比对 create-product 包（创建向导已覆盖，本包从向导创建后的阶段页开始）与 product-management 包（列表与入口跳转用例已覆盖）。
>
> 上下文结论：①阶段容器（/integration/product/{id}/basic，2026-09-03 探索实证 product id=2095414624647254016/at8438）：PageWrapper 标题=产品名称+开发状态 tag+「切换产品」icon 链接（弹窗：关键字搜索/产品列表/点击切换 URL），描述区 ProductMetaInfo 五项（Model/开发方式/品类/通讯方式/更新时间），操作区「申请上线」（BtnPublishProduct 抽屉，两步：确认提审项[各阶段 必填/选填 + 已完成/未完成 tag + 去配置/查看] / 申请上线[产品 meta + 协议确认]；未就绪时禁用）；五步步骤条（基本配置/功能定义/设备开发/高级配置/产品测试，2026-09-03 执行实证；Interactive 不在步骤条）；页脚 上一步/保存（首阶段）/取消；②基础配置表单：只读（Model/产品型号/设备类型/语音控制 disabled），可编辑（产品名称 3-60 必填、配网方式 select+查看详情 link、条件字段 WiFi/蓝牙名称 3-24 [requireApName] 与 可挂载的网关 [parentGatewaies 多选]、厂商标识1/2 max64、详情页URL max255、产品拟物图/配网引导图 file-uploader 2MB 1080x1080 透明 PNG、配网文案 200 必填 textarea；企业品牌已注释不渲染）；③保存链路 saveStage：表单校验失败或保存异常时弹「您有修改还未保存。继续切换页面将导致修改内容丢失。」（留在页面/切换页面）；必填项含 产品名称/配网方式/产品拟物图/配网引导图/配网文案——向导创建的全新产品实证缺 配网方式/两张图/配网文案，直接保存会被必填校验拦截；「申请上线」按钮对新制品为可点击（抽屉内引导）；④写入目标为台账最新产品（当前 at8438，生命周期约定见 product-management conclusion），台账新 kind=open-platform-product-basic-edit 记录编辑与保存。
>
> 范围外声明：申请上线抽屉提交（依赖五阶段全部完成，测试产品保持开发态，本包仅断言按钮存在与禁用态）；可挂载的网关/WiFi蓝牙名称条件字段联动（依赖特定配网方式选择与网关产品数据，条件组合覆盖留待后续）；产品拟物图/配网引导图的尺寸与格式服务端校验（1080x1080 透明底 PNG 合成图上传留作 OP-PBSC-009 执行观察，不作为独立用例断言）；阶段间下一步导航链路（设备开发/高级配置/产品测试由后续包覆盖）。
>
> 本表供测试范围审核。真实密码、Token、密钥、Cookie 等可复用凭据仅从本地 `.env` 或对应登录态存储读取；测试产品与编辑值均为本测试体系生成的合成数据。

## 快速索引

| 模块 | 用例编号 | 用例标题 | 优先级 | 风险 |
| --- | --- | --- | --- | --- |
| 阶段容器 | OP-PBSC-001 | 阶段容器渲染与产品信息条 | P0 | 低 |
| 基础配置表单 | OP-PBSC-002 | 只读字段与可编辑字段在位 | P1 | 中 |
| 基础配置表单 | OP-PBSC-003 | 配网方式查看详情弹窗 | P1 | 低 |
| 基础配置表单 | OP-PBSC-004 | 产品名称校验边界 | P1 | 中 |
| 基础配置表单 | OP-PBSC-005 | 超长字段截断（厂商标识/详情页URL） | P2 | 低 |
| 阶段容器 | OP-PBSC-006 | 切换产品弹窗与产品切换 | P1 | 中 |
| 保存与守卫 | OP-PBSC-007 | 未保存守卫弹窗（留在页面/切换页面） | P1 | 中 |
| 保存与守卫 | OP-PBSC-008 | 保存必填校验拦截（不写入） | P1 | 中 |
| 保存与守卫 | OP-PBSC-009 | 补齐必填并保存成功（写入） | P1 | 高 |

## 模块：阶段容器

<details>
<summary>OP-PBSC-001｜阶段容器渲染与产品信息条｜P0｜低风险</summary>

> 前置条件：已登录；台账最新产品存在（生命周期约定：create-product 包每次运行补充）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 列表点击台账最新产品「继续开发」 | 台账最新产品 | 进入 /integration/product/{id}/basic，页面标题显示该产品名称 |
| — | 2 | 观察状态与信息条 | 无 | 开发状态 tag（开发中）可见；MetaInfo 五项（Model/开发方式/品类/通讯方式/更新时间）与台账一致 |
| — | 3 | 观察步骤条与页脚 | 无 | 五步步骤条「基本配置/功能定义/设备开发/高级配置/产品测试」且基本配置高亮（实证 ✓）；页脚 下一步/保存/取消 可见、上一步不渲染（首阶段，源码+实证） |
| — | 4 | 观察申请上线按钮 | 无 | 「申请上线」按钮存在且可点击（实证 ✓：未就绪产品也开放入口，提审确认抽屉内以各阶段完成度引导；本包不提交） |

</details>

## 模块：基础配置表单

<details>
<summary>OP-PBSC-002｜只读字段与可编辑字段在位｜P1｜中风险</summary>

> 前置条件：已登录，位于台账最新产品基础配置页。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察只读字段 | 无 | Model/产品型号 输入框 disabled 且值与台账一致；设备类型/语音控制 select disabled |
| — | 2 | 观察可编辑字段 | 无 | 产品名称（回显+字数统计 n/60）、配网方式控件在位（新产品为 placeholder「请选择配网方式」；若历史运行已保存则为已选值——状态注解记录）、厂商标识1/2、详情页URL、产品拟物图、配网引导图、配网文案 控件在位 |
| — | 3 | 观察条件字段 | 无 | WiFi/蓝牙名称与可挂载的网关按当前配网方式条件显示（探索复核并注解实际可见性） |

</details>

<details>
<summary>OP-PBSC-003｜配网方式查看详情弹窗｜P1｜低风险</summary>

> 前置条件：已登录，位于基础配置页。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点击配网方式「查看详情」 | 无 | 弹窗标题「联网方式和配网方式对应关系表」 |
| — | 2 | 观察表结构 | 无 | 表头 联网方式/配置项/配置选项/说明；含 wifi 分组行（rowspan）与 4G/蜂窝 等联网方式行 |
| — | 3 | 关闭弹窗 | 无 | 弹窗关闭，页面可继续操作 |

</details>

<details>
<summary>OP-PBSC-004｜产品名称校验边界｜P1｜中风险</summary>

> 前置条件：已登录，位于基础配置页；记录原产品名称用于还原。
>
> 是否写入数据：否（校验不保存，结束前还原输入框为原值且不触发保存）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| D01 | 1 | 清空产品名称触发校验 | 空 | 必填错误提示出现 |
| D02 | 2 | 输入 2 字触发校验 | 合成值「测试」 | 前端不强制最小长度（2026-09-03 源码实证：productName 长度校验为空实现，minlength 仅为 DOM 属性），不出现长度错误；服务端是否限制由保存链路另行实证（注解） |
| D03 | 3 | 输入 61 字 | 合成值（61 个汉字） | maxlength=60 DOM 截断生效（输入保留 60 字），注解记录 |
| D04 | 4 | 还原为原产品名称 | 原产品名称 | 错误提示消失，输入框回显原值，不保存 |

</details>

<details>
<summary>OP-PBSC-005｜超长字段截断（厂商标识/详情页URL）｜P2｜低风险</summary>

> 前置条件：已登录，位于基础配置页。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 厂商标识1 输入 70 字符 | 合成值（70 字符） | maxlength=64 生效：输入截断为 64 字符（或超长提示，以实证为准） |
| — | 2 | 详情页URL 输入 260 字符 | 合成值（260 字符） | maxlength=255 生效：输入截断为 255 字符（或超长提示，以实证为准） |
| — | 3 | 清空两字段还原 | 清空 | 输入框恢复原值，不保存 |

</details>

<details>
<summary>OP-PBSC-006｜切换产品弹窗与产品切换｜P1｜中风险</summary>

> 前置条件：已登录，位于台账最新产品基础配置页；列表存在 ≥2 个产品。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 点击标题区切换产品图标 | 无 | 弹窗「切换产品」打开，产品列表渲染（名称+Model+开发方式+通讯方式） |
| — | 2 | 关键字输入不存在的串 | 合成值 zzz-not-exist | 空态提示「没有找到产品」 |
| — | 3 | 关键字清空后选择另一产品行 | 台账另一产品 | URL 变为 /integration/product/{另一id}/basic，标题/信息条随切换 |
| — | 4 | 切回台账最新产品 | 台账最新产品 | 回到最新产品基础配置页 |

</details>

## 模块：保存与守卫

<details>
<summary>OP-PBSC-007｜未保存守卫弹窗（留在页面/切换页面）｜P1｜中风险</summary>

> 前置条件：已登录，位于基本配置页。
>
> 是否写入数据：否（表单置为非法值触发守卫，非法值不会提交服务端；「切换页面」后的阶段完成度拦截为 toast，不产生写入）。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 产品名称改为 2 字后点击「下一步」（阶段切换走 saveStage 守卫；2 字可通过前端校验、由服务端拒绝） | 合成值「测试」 | 守卫弹窗出现，文案为「您有修改还未保存。…」或「自动保存时发生错误: …，继续切换页面将导致内容丢失。」（服务端拒绝分支，2026-09-03 实证），按钮 留在页面/切换页面 |
| — | 2 | 点击「留在页面」 | 无 | 弹窗关闭，仍在基础配置页 |
| — | 3 | 再次触发并点击「切换页面」 | 无 | 弹窗关闭；若阶段完成度不足会被「请先完成…」toast 拦截停留本页（源码 validateStageCompleteStatus，执行实证注解最终落点） |

</details>

<details>
<summary>OP-PBSC-008｜保存必填校验拦截（不写入）｜P1｜中风险</summary>

> 前置条件：已登录，位于台账最新产品基本配置页；该产品为新创建合成产品（通常缺 拟物图/配网文案）。
>
> 是否写入数据：否。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 1 | 观察必填项现状 | 无 | 产品拟物图/配网引导图/配网文案 的已完成状态以实证注解（新产品通常为空） |
| — | 2 | 直接点击保存（表单存在必填缺失时） | 无 | 表单内联必填错误出现 + toast 错误提示（「请修改表单中的错误」类，2026-09-03 执行实证：保存按钮走 safeSaveConfig 仅 toast，不弹守卫），无「配置保存成功」提示 |
| — | 3 | 弹出守卫时选择「留在页面」 | 无 | 仍在本页，服务端无写入（URL 与数据不变） |

</details>

<details>
<summary>OP-PBSC-009｜补齐必填并保存成功（写入）｜P1｜高风险</summary>

> 前置条件：已登录，位于台账最新产品基本配置页；准备 1080x1080 透明底 PNG 合成图（本地生成，<2MB）。保存对象仅限台账最新合成产品。
>
> 是否写入数据：是（为台账最新产品补齐 配网文案 与缺失图片并保存；成功后记入本包台账 kind=open-platform-product-basic-edit，只增不删）。若服务端拒绝合成图（尺寸/格式校验失败），以错误提示实证为准转注解，不重试爆破。
>
> 脚本状态：待审核。

| 数据编号 | 步骤 | 操作 | 测试数据 | 预期结果 |
| --- | --- | --- | --- | --- |
| — | 0 | 选择配网方式 | 字典实际项（新产品配网方式为空，实证 ✓；选 WiFi 类选项，WiFi/蓝牙名称条件字段如出现则注解） | 配网方式回显所选值 |
| — | 1 | 补填配网文案 | 合成文案「自动化测试配网文案（product-basic 写入用例）」 | 输入成功，字数统计生效（≤200） |
| — | 2 | 补传产品拟物图/配网引导图（若缺失） | 本地合成 1080x1080 透明 PNG | 上传成功，缩略图卡片出现 |
| — | 3 | 点击保存 | 无 | toast「配置保存成功」出现（源码 ElMessage.success）；无守卫弹窗、无内联错误 |
| — | 4 | 台账记录 | — | 本包 runtime/generated-data.json 追加编辑保存记录（产品名/Model/字段/保存时间） |

</details>
