# 开放平台产品功能定义：测试结论

## 当前结论（2026-09-03 首轮建立）

- 用例集 7 条（写入 2 条），7/7 全部通过（2026-09-03，一轮修正后）。上下文来源与范围外声明见 cases.md 顶部。
- 2026-09-03 探索与执行实证（at0635.light.at8438）：
  - 模板卡片 7 张：「自定义功能」（自定义 badge，排第一）+ 6 个标准模板（灯带恒压驱动器/台灯/筒射灯恒流驱动器/筒灯/床头灯/灯带）。
  - 选择标准模板「灯带恒压驱动器」→ toast「功能模板保存成功」→ 必选+可选功能点自动加入：属性（断电勿扰/色温/开关/亮度…）、事件、动作三列表均有数据。
  - 功能列表列头：功能点名称/标识符code/功能类型/数据类型/数据传输类型/场景权限/数据定义/操作（详情+删除，删除仅 non-Required 行）。
  - 「点我查看」为 router-link target=_blank → /service-support/home?path=…define。
  - function 页页脚为 上一步/下一步/取消（无保存按钮；保存仅 basic 首阶段有）。

## 实现差异与经验（已回写用例表）

1. **动作列表标题是「方法 / Action」**（源码 FuncActionList 使用 func-property-title 类但文案为「方法」）——不是需求常见的「动作」，已按实证修正用例表。
2. 功能点删除走 预检（queryProductFunctionUsageInfo）→「删除确认」ElMessageBox → toast「功能已删除，请及时保存更改。」，删除即时生效（无二次保存步骤）。
3. DrawerTrigger 的 `.drawer-trigger` 是常驻 wrapper（含触发按钮），开合断言必须用 `.ep-drawer` 面板可见性。
4. 「重新选择」仅清客户端选中态回卡片栅格；未选模板点「下一步」触发 saveStage 守卫「您还没有选择功能点，确定要切换页面吗？」（源码句尾双句号，实证一致）。

## 写入与数据说明

- 台账 4 条（kind=open-platform-product-func-tpl / open-platform-product-func-delete，只增不删）：模板选择×2（首跑+幂等重跑）、功能点删除×2（首跑删「断电勿扰」、重跑重新选模板后再删）。
- 幂等设计：003/006 依赖「已选模板」时先点「重新选择」回栅格再重选标准模板（服务端覆盖写入，可逆）；006 每次选模板后第一个带删除链接的功能点（首跑为「断电勿扰」）。

## 后续关注

- 自定义模板（code=custom）的自建功能点链路（自定义属性/事件/方法弹窗）为范围外，如需覆盖另立用例。
- 功能点「添加属性」批量添加弹窗内部校验未覆盖（范围外）。

## 2026-09-14 复测（product-edit-chain-20260910，product-function）

- 四轮执行历程、全链路基准（R3 全绿）、状态推进说明与已知差异见 `testpacks/web/open-platform/test-reports/product-edit-chain-20260910.md`「执行历程与多轮说明」。
- 本包涉及的最新页面事实与脚本修订均已在 R3 全链路验证通过；R4 的失败为 at0672 送审态（R3 链路推进的业务结果）下的只读锁定，非本包用例/脚本缺陷。

### 2026-09-14 复测收官（R5–R7）
- R5/R7 链路跑在开发态新产品上：11/11 全绿（模板选择/功能点增删/交互配置跳转全通过）。R4 的送审态模板锁定失败未复现（状态守卫 skipIfTemplateLocked 作为休眠保险保留：6 处模板/功能点类用例在模板区受限时自动 skip+注解）。
- 已知差异：无新增。
