# 开放平台创建产品：测试结论

## 当前结论（2026-09-07 复测全量通过）

- 2026-09-07 用户请求复测「产品创建和删除」，与本轮 product-management 包（删除用例所在）合并为同一请求报告 `product-create-delete-retest-20260907`。
- 执行结果：**13/13 全部通过（1.5 分钟）**。登录态复用生效（不发送短信、零人工介入）；004 品类重试机制触发 1 次后稳定。
- 本轮写入：OP-PROD-011 创建 1 个产品（自动化测试产品1788751759300 / at9300 / model `at0635.light.at9300`），台账已记录；010/013 重复型号/名称均被后端拒绝（提示文案见报告注解：型号重复提示文案为「不能与本企业同品类下的其他产品型号重复」，与旧记录「产品型号重复」存在文案差异，语义一致）。
- 环境注意：健康探测在 dev server 未运行时会以 fetch failed 误报「卡顿窗口」（探测先于 Playwright webServer 自动启动执行）；此时按提示设 `FAST_SKIP_HEALTH_PROBE=1` 由 webServer 冷启动即可，beforeAll 预热可吸收冷启动。
- 测试环境：test（本地 Vite `http://127.0.0.1:3098/`，API 直连 `gateway-test.ikingcity.com` 测试后端）。

## 历史结论（2026-09-02 对象矩阵核对后全量通过）

- 2026-09-02 按对象矩阵核对规则（commit `3ee600a` 最新规范）复核用例集：**新增 1 条用例 OP-PROD-013（重复产品名称创建被拒绝，P1 高风险）**，并为 003/005/008 补充核对发现的新步骤；用例集 13 条（评审模型 48 行执行行），工作簿重新导出并经用户确认后执行。
  - 003 +步骤4/5：已登录绕过直达创建页（向导初始态一致）+【返回】回产品开发首页（源码核实 `goList → integration-product-management`）。
  - 005 +步骤5：同品类切换方案表单联动。**探索实证修正预期**：「照明-灯」下两方案通讯协议集合相同（均 11 项），集合差异不成立；可观察联动信号 = 协议选项集合非空（随 `supportedProtocol` 刷新）+「协议类型」字段按方案显隐。运行实测开放协议 11 项 / 云云 11 项，字段显隐正确。
  - 008 +步骤5：描述选填留空实证（无必填提示，源码 `describe` 规则无 required）。
  - 013：重复名称提交被拒绝，提示「产品名称重复」与源码错误码 10524 一致，无跳转、无意外创建。
- 执行结果：全量首跑 **10/13 通过**（004/008/010 因 dev server 卡顿窗口超时失败），**单独重跑 3 条全部通过（43.8s）**——同一代码同一环境复跑即过，证实为环境性卡顿而非功能缺陷；功能层面 **13/13 全部验证通过**。
- 本轮脚本稳定性修正（运行实证沉淀）：
  - 创建页静态字典（`S_INTEGRATION_DEVELOP_TYPE` 等）经 Vite 动态 import 加载，冷启动可达约 9s，未就绪时方案卡片/协议选项不渲染——`selectDevelopPlan` 内硬等 radiogroup 就绪、方案可见断言放宽至 25s。
  - 003 入口点击在首页资源未就绪时会丢失跳转（实测约 8s 空窗）——补按钮可见 + load 等待。
  - 新 Allure 配置（video+screenshot on）下 `page.screenshot` 会等待 `document.fonts.ready`，字体挂起时截图超时误报失败——`attachShot` 先等字体就绪、失败降级重试一次。
  - 耗时优化（2026-09-02）：SPA 导航改用 `domcontentloaded` + 20s 快速失败重载一次（`gotoWithRetry`，原等 load 事件在卡顿窗口会拖满 60s）；新增 `beforeAll` 套件预热（整轮一次，提前触发 Vite 字典模块按需编译；dev server 卡顿在预热阶段约 45s 内暴露，而非拖垮多条用例各 15 分钟）。验证：无写入 4 条（003/004/008/010）57.4s 全部通过；**2026-09-03 全量 13 条回归通过（1.6 分钟）**，产物降载配置（通过用例仅关键截图、失败留截图/录屏/trace）与跑前健康探测均在位，011 写入 1 个产品（at5356）台账闭环。
- 测试环境：test（本地 Vite `http://127.0.0.1:3098/`，API 直连 `gateway-test.ikingcity.com` 测试后端）；报告产物含 Playwright HTML 报告（`artifacts/report/`）与 Allure 结果（`artifacts/allure-results/`、`allure-report/`）。

## 历史结论（2026-09-01 全量通过）

- 2026-09-01 按最新规范重新设计用例：基于前端源码实现核对（`ProductCreate.vue`、`ProductManagement.vue`、`IptProductCategorySelect.vue`、`ValidationRuleBuilder.ts`、路由与权限），用例集 12 条（七类提取全覆盖），审核工作簿经用户确认后执行。
- 全量执行 **12/12 通过**。`npm run test:fast -- testpacks/web/open-platform/create-product/create-product.spec.ts`。
- 测试环境：test（本地 Vite `http://127.0.0.1:3098/` test 模式，API 直连 `gateway-test.ikingcity.com` 测试后端）。
- **登录态复用策略**：登录功能本身由 `login-register` 功能包覆盖，本包登录仅为前置条件。OP-PROD-002 优先复用 `runtime/auth-state.json`（token cookie，仅存本地不入 Git）中的有效会话——会话有效则跳过短信登录（不发送短信、无需人工点选验证码，002 仅约 4 秒）；仅当会话缺失/过期时才短信登录并刷新该文件。003~012 一律经 storageState 复用该文件。会话有效时全量跑约 1.5 分钟、零人工介入。
- 说明：每轮全量执行 OP-PROD-011 会真实创建 1 个产品（写入用例）；如只想回归校验类用例，可 `--grep "OP-PROD-00[1-9]|OP-PROD-012"` 跳过写入。

## 执行中发现并处理的问题

1. **品类数据偶发不完整**：`queryProductCategoryList` 偶发返回缺 children 的数据，选中一级品类后二级区域为空。脚本在 `selectCategory` 中加重试（重选一级品类，最多 3 次）后稳定。
2. **产品列表字典未命中显示原始值（真实产品缺陷候选，已注解记录）**：开发状态/开发方式/通讯方式/设备类型列显示 `developing/direct/wifi/ordinary` 等原始枚举值而非中文「开发中/开放协议接入/WiFi/普通设备」；`colorful-tag`（字典 `S_INTEGRATION_ACCESS_STATUS` 等）未命中时回退显示原始值。语义与「开发中」一致，OP-PROD-012 以「文案差异」注解记录，建议反馈产品/前端确认字典加载或取值问题。
3. **型号格式提示文案不在通用提示关键词表内**：`仅支持小写字母或数字，最多6位` 不含常见关键词，捕获关键词表已补充「仅支持」。
4. **（2026-09-02）创建页静态字典冷加载竞态**：方案卡片/协议选项渲染依赖 Vite 动态 import 的 `.dict.ts` 模块（`S_INTEGRATION_DEVELOP_TYPE` 等），冷启动首次加载实测可达约 9s；未就绪时页面无报错、仅表现为方案区域空白/radiogroup 缺失，`developTypeOpts` 过滤为空。已按「等待渲染就绪信号」修正脚本（见当前结论）。开发侧建议：字典加载失败或延迟时给出 loading 态，避免空白误导。
5. **（2026-09-02）dev server 间歇卡顿窗口**：Vite dev 长时间运行后出现渲染/网络停顿（页面 CDP 调用变慢、`load` 事件 60s 不触发、首跳转约 8s 空窗），同用例复跑即恢复；首轮 3 条超时失败均落在此窗口，与功能逻辑无关。

## 实现与需求的已知差异（源码核对 + 执行实证）

1. 产品名称「不允许标点符号和空格」的字符集校验在 `ValidationRuleBuilder.productName()` 中已整段注释，前端不拦截（OP-PROD-008 执行实证：含空格标点名称无格式提示）。
2. 产品描述需求为「最多200字符」，实现为非必填但填写时长度 3~200（2 字符报「描述长度必须在3~200之间」，OP-PROD-008 实证一致）。
3. 实现新增必填「通讯协议」单选（开放协议接入下 11 项：WiFi/蓝牙/NB-IoT/Zigbee/WiFi+蓝牙/4G/RS485/PLC/以太网/Z-wave/LoRa）与云云接入专属必填「协议类型」（平台标准协议/自定义协议）。
4. 创建成功信号：toast「产品创建成功」+ 自动跳转 `/integration/product/{productId}/basic` 基础配置页（非需求描述的返回列表场景）。
5. 创建为单页三段向导（无「下一步」按钮）：提交按钮可用条件=品类+方案已选，点击时才触发字段校验（OP-PROD-006 实证拦截提示「请输入产品名称/请选择通讯协议/请选择设备类型」）。
6. 产品 model 组成实证：`at0635（企业标识）.light（品类英文）.at5948（产品型号）`，与需求「企业标识+二级品类英文+产品型号」一致。
7. 型号唯一性后端拒绝实证：同品类重复型号提交返回错误码 10516，型号字段提示「产品型号重复」，无写入（OP-PROD-010 实证）。

## 测试数据与台账

- 2026-09-01 两轮 + 2026-09-02 三轮执行，共创建 5 个产品（照明-灯 / 开放协议接入 / 普通设备），全量明文记录于本功能包 `runtime/generated-data.json`（只增不删）：
  - 自动化测试产品1788254235948 / 型号 at5948 / model `at0635.light.at5948`（2026-09-01）
  - 自动化测试产品1788254524129 / 型号 at4129 / model `at0635.light.at4129`（2026-09-01）
  - 自动化测试产品1788255508406 / 型号 at8406 / model `at0635.light.at8406`（2026-09-01）
  - 自动化测试产品1788319245262 / 型号 at5262（2026-09-02 首轮）
  - 自动化测试产品1788321497426 / 型号 at7426（2026-09-02 被中止的第二轮运行在终止生效前完成的 011 创建）
  - 自动化测试产品1788324657739 / 型号 at7739 / model `at0635.light.at7739`（2026-09-02 终轮，012 已列表回查）
- 重复型号尝试（OP-PROD-010）与重复名称尝试（OP-PROD-013）均被拒绝，未入台账；产品按约定不删除，保留在测试企业内。

## 风险与限制

- 登录（短信+图形验证码人工协助）与创建产品提交为外部写入，已获用户确认后执行（2026-09-01）。
- 范围外（用例表头部已声明）：双击幂等、企业 40 个产品上限、文件上传（无控件）、验证码细节（属 login-register 包）、产品删除清理。
- 品类一级标签与二级卡片为纯文本节点（无 button/radio role），脚本按可访问文本 + 项目自有类（`.product-type-item`、`.plan-item`、`.cate-selected`）定位；云云接入「协议类型」label 内含 DotTip 提示组件，可访问名称非精确文本，按子串匹配定位。
