# 开放平台产品开发列表：测试结论

## 当前结论（2026-09-07 复测通过，OP-PMGT-008 候选规则修订）

- 2026-09-07 用户请求复测「产品创建和删除」，与 create-product 包合并为同一请求报告 `product-create-delete-retest-20260907`（复测 5 次追加执行，汇总页按用例取最终状态）。
- 执行结果：**8/8 全部通过**。001~007 首轮即通过；008 首轮失败（见下），修订后通过。登录态复用生效；005 本轮实证下载事件出现（`产品列表-2026-09-07.xlsx`，此前 09-03 记录为「仅提示无下载事件」，环境行为已恢复）。
- **008 首轮失败根因（数据状态漂移，非产品缺陷）**：候选 at8438 已于 09-03 17:18 被五步阶段包推进到「送审中」，操作列仅「开发详情」——「删除」仅 开发中/测试未通过 可见（与用例既有认知一致，但旧候选规则未校验）。修订：候选新增「行操作列可见删除」条件，并放宽「台账 Model 必填」（2026-09-02 批次 at5262/at7426 台账缺 Model 但平台已生成 Model 且可删；Model 于删除前从行内单元格取实测值入台账）。用例表与审核工作簿已同步修订并经用户确认后执行。
- **连带修正（脚本缺陷）**：①`readCreateProductLedger` 原在读取层过滤无 Model 记录，008 改读完整台账（001/002/007 维持原行为）；②台账 Model 实测值必须在点击删除前读取——删除后行即消失，删除后读取会超时（首轮修订版在步骤 3 栽在此处，at7426 已删除但记录未写入，已按实际写入补记台账）。
- 本轮写入：删除 2 个合成产品——at7426（自动化测试产品1788321497426，补记）与 at5262（自动化测试产品1788319245262，Model 实测 `at0635.light.at5262`），均入台账（现 7 条删除记录）。
- 工具修复：runner 的 Allure 报告生成改用 Allure 3 语法（移除 Allure 2 的 `--clean`，改为生成前手动清理输出目录）。
- 测试环境：test（本地 Vite `http://127.0.0.1:3098/`，API 直连测试后端）。

## 历史结论（2026-09-03 首轮建立）

- 用例集 8 条（写入 1 条），全部可执行。上下文来源与范围外声明见 cases.md 顶部。
- 与 create-product 包查重划界：创建向导主流程、首页基础渲染与列表回查已由该包覆盖；本包聚焦列表查询/筛选/排序/导出/操作列跳转/删除。
- 2026-09-03 探索实证（只读探索，快照与截图见 `runtime/exploration/out/`）：
  - 列表 7 行 = create-product 台账 7 产品；首行（最新 at5356）：开发中、操作列「继续开发 | 删除」、Model at0635.light.at5356、通讯 WiFi、设备类型 普通设备。
  - 关键字搜索：名称片段命中 1 行、型号片段命中、不存在串 0 行、清空恢复 7 行（回车触发查询）。
  - 筛选器：品类 12 tabs（插座开关/网关路由/照明/传感器/家居安防/厨房电器/影音娱乐/环境电器/家居生活/清洁电器/健康运动/其他）+ 开发状态 7 项（开发中/送审中/测试中/测试未通过/测试通过未上线/上线审核中/已上线）+ 开发方式 4 项 + 通讯方式 11 项 + 设备类型 3 项；底部 取消/确定。
  - 侧边菜单：「产品接入」组（产品开发/授权码管理/采购管理/协议管理）。
  - el-table 渲染多个 table（含隐藏测量表），自动化定位用 `.ep-table__body:visible`。
- 排序字典项（S_PRODUCT_TIME_SORT）在探索中以下拉打开确认 ≥2 项；具体文案以执行轮注解。

## 写入与数据说明

- 写入用例 OP-PMGT-008 的候选规则（2026-09-03 执行轮修订并实证）：台账中 Model 已生成 + 当前列表可见 + **非台账最新产品**；每轮执行删除一个较旧合成产品并记入本包台账（kind=open-platform-product-deletion，只增不删，已累计 5 条：7739/8406/4129/5948/5356）。
- **产品生命周期约定**：create-product 包每次运行新增 1 个产品（成为台账最新、受 008 保护，供产品五步阶段包使用）；product-management 每次运行删除 1 个较旧产品。候选耗尽时 008 明确报错并提示重跑 create-product 功能包补充——两个包构成可持续的创建/清理循环。
- 台账存在过期条目（如 7739 曾在台账但早于本包执行就不在平台列表）——008 以「台账 ∩ 当前列表」交集为准，不盲信台账。
- 导出 Excel 为读操作：断言「导出成功」提示并观察下载事件（test 环境仅返回成功提示、无浏览器下载事件，注解记录）。

## 实现与需求的已知差异（探索实证）

1. el-table 隐藏测量表与固定列拆表导致 DOM 中存在多个 table——非缺陷，记录为自动化定位经验。
2. 无其他实现差异（本包探索未发现文案/交互与需求冲突项）。

## 后续关注

- 删除产品接口为 `deleteProductConfig`（源码中残留 TODO 注释「调用删除接口」，实际已调用，功能可用）——如线上反馈删除异常可回查此处。
- 产品五步阶段（basic/function/develop/advanced/testing）由后续包覆盖，入口已在 OP-PMGT-007 验证。
