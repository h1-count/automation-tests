{
  "description": "Project-local adapter from a Codex Stop event to the request workflow gate.",
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"$(git rev-parse --show-toplevel)/scripts/codex-stop-stage-envelope.mjs\"",
            "timeout": 30,
            "statusMessage": "Checking workflow reply eligibility"
          }
        ]
      }
    ]
  }
}
